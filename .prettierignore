node_modules/
dist/
target/
pnpm-lock.yaml
src-tauri/gen/
src-tauri/icons/

# Byte-exact subtitle test data: a formatter rewriting these would rewrite the tests. See BACKLOG.md M1.1.
fixtures/subtitles/

# Owner-maintained governance docs: tooling must not rewrite them.
CONTRIBUTING.md
BACKLOG.md

# The whisper.cpp checkout and its build trees: not ours to format. See BACKLOG.md M3.1.
.whisper/

# A byte-exact capture of a real whisper run, and hand-written word lists. See BACKLOG.md M3.3.
fixtures/asr/

# Local tooling and its output: agent worktrees, the OMC state directory, and the logs the CI
# scripts write when they are run by hand. None of it is the product, and CI never sees any of it.
.claude/
.omc/
ci-logs/

