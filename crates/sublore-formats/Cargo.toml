[package]
name = "sublore-formats"
version = "0.1.0"
description = "Lossless subtitle parsing and serialization"
edition = "2021"
license = "GPL-3.0-or-later"
publish = false

# Deliberately empty: the parsers are std-only, and a dependency here would have to be
# justified as a public-interface change. See BACKLOG.md M1.1.
[dependencies]
