# BACKLOG.md — Sublore build plan

Ordered milestones for v1.0 (scope: CONTRIBUTING.md §1). Rules: work top to bottom; a milestone's tasks are detailed when the milestone starts, using M0 as the template; every task gets acceptance criteria BEFORE implementation; the owner's checklist at the end of each milestone is the definition of done.

Status legend: `[ ]` open · `[~]` in progress · `[x]` verified-by-tests · `[✓]` owner-passed · `[!]` blocked

Behavioural verdicts in this file are **verified on Linux** unless they say otherwise. Windows compiles in CI; it is not verified until MW closes (CONTRIBUTING.md platform policy, 2026-08-29).

The planning documents this file cites — `decisions.md`, `shell-layout.md`, `shell-mockup.html`, `m2-0-tasks.md`, `post-v1-plan.md`, `x11-vs-render-api.md` and the ASR research sweeps — left this repository on 2026-09-01 for the owner's private planning archive. They are named here, never given a path: `docs/design/` no longer exists.

**Decisions in force** (owner ruling 2026-08-29, full text in `decisions.md`):

| # | Decision | Lands in |
|---|---|---|
| 1 | Video occlusion: native surface hides for HTML layers | M2.0 |
| 2 | Show and re-show with a loaded video, built now | N2 |
| 3 | Windows E2E backend | MW.1a (done), MW.1b, MW.1c |
| 4 | Composite history entry, one undo per operation | M2.5 (shape), M7 (use) |
| 5 | Active line and selection separated | M2.0 |
| 6 | Text matcher in the open core | M5 |
| 7 | Video shows the translation, source on toggle, shadow copy | M2.6 |
| 8 | Autosave in its own store, backups untouchable by timers | M11 (post-v1) |
| 9 | Close gate — active defect | N1 |
| 10 | Non-cue segments: written analysis now | N3 |
| 11 | Milliseconds, final. No frame vocabulary | M2.5 note, plan-wide |
| 12 | M2.4 reuses the ASR pattern, not its code | M2.4 |
| 13 | New translation from source | M2.6 |

---

## M0 — Skeleton that proves the stack (fully specified, start here)

Goal: a running Tauri 2 app on Windows and Linux that plays a video with libmpv and has green CI. This milestone exists to kill stack risk first: if libmpv embedding fails, we learn it in week one, not month three.

- [~] **M0.1 Repo bootstrap.** Tauri 2 + Rust core + TS frontend, workspace layout, lint/format hooks, README stub.
  - AC: `npm run tauri dev` opens a window titled Sublore on Windows and Linux; CI runs lint + build on both and is green.
  - Status 2026-08-22: implementation merged to `main` locally; full check battery (install, format, lint, build, fmt, clippy, cargo build) plus live app launch with window title verified on Linux. Remote publication deferred by owner, so the CI-green and Windows halves of the AC are unverified.
- [~] **M0.2 libmpv embedding.** Video renders inside the app window via libmpv, with play/pause and seek.
  - AC: open fixture `fixtures/video/sample.mkv`; video plays with audio; pause, seek to 0:30, frame changes accordingly; app closes cleanly with no orphan process.
  - Status 2026-08-22: implemented (libmpv2 crate, X11 wid embedding via native child window, XWayland on Wayland); 14 behavioral tests green against real libmpv; independent live verification under Xvfb passed every AC (video advances, pause freezes byte-identical, seek lands at 30.0, WM_DELETE_WINDOW close exits 0 with no orphans). Audio verified to decode (AAC track selected); audibility needs the owner's speakers. Windows and CI runs still unverified (no remote, no Windows machine).
- [~] **M0.3 Packaging smoke.** CI produces installable artifacts (msi/exe, deb + AppImage).
  - AC: artifact from CI installs and runs M0.2's behavior on a clean VM of each OS.
  - **The AC splits by platform and only the Linux half can be answered today.** A `package-linux` job builds one bundle per runner and uploads one artifact each, deb, rpm and AppImage; a `package-windows` job builds the msi and the exe. A `package-smoke` job installs the deb in a clean `ubuntu:24.04` container, starts what it installed and then the AppImage beside it under Xvfb, and a `package-smoke-rpm` job does the same for the rpm in a clean `fedora:latest`. Both are red unless the 1024x700 "Sublore" toplevel comes up mapped. It is `.github/scripts/package-smoke.sh` and a Node check that reads the window through `e2e/lib/x11.js`, the same seam MW.1b replaces. Nothing installs or starts the Windows artifacts: no Windows behaviour has ever been run here, so the msi and the exe are files this workflow produces and nothing more. **Installing and running them is MW.3's**, by hand, from the installed build. One thing to expect there, read off the config rather than run: nothing puts `libmpv-2.dll` beside the installed exe, and the app links it, so the first Windows install will have no libmpv to load.
  - Status 2026-09-02, verified on Linux, locally and not in CI: the path was replicated in containers rather than trusted. An `ubuntu:24.04` container with the workflow's apt list built both bundles, and a second, clean `ubuntu:24.04` installed the deb and started both artifacts, 3 checks each. The smoke was proven to bite: a deb built without the libmpv dependency installs and then dies with `libmpv.so.2: cannot open shared object file` (exit 127), and a missing binary and a window title the app does not have both fail too. **CI has never run any of this** — nothing has been pushed — so the artifact names, the `container:` job and the upload/download pair are unverified.
  - Status 2026-09-03, unverified: the three Linux bundles were split onto a runner each and the rpm was added (owner ruling). The rpm bundler is `rpm-rs` inside the Tauri CLI, so no `rpmbuild` is needed, and it writes `libwebkit2gtk-4.1.so.0()(64bit)` and `libgtk-3.so.0()(64bit)` into Requires by itself but nothing for libmpv; `tauri.conf.json` declares that one by soname, which is what both Fedora's `mpv-libs` and openSUSE's `libmpv2` provide. Built and its Requires read on Fedora 44 locally. **Nothing has installed or started an rpm anywhere**, so `package-smoke-rpm` is a check nobody has seen pass.
  - Found on the way: the deb declared no libmpv dependency at all, so it installed anywhere and then failed to start; `tauri.conf.json` now declares `libmpv2`. The AppImage needs `librsvg2-dev` at build time for its gtk plugin, and leaves `libOpenGL.so.0` to the host, which a bare image has to install. Building the AppImage on Fedora also needs `NO_STRIP=true`, because linuxdeploy's bundled strip is older than `.relr.dyn`; the runner's Ubuntu does not, so the workflow does not set it.
- [~] **M0.4 Crash safety baseline.** Panic/error handler; log file in app data dir; no console spam in release.
  - AC: forced error path shows an actionable dialog and writes the log; app restarts normally after.
  - Input from M0.2 (2026-08-22): an external client hard-destroying the toplevel via XDestroyWindow segfaults the app in libgobject (normal WM_DELETE_WINDOW close is clean, exit 0). Not a user-reachable path through normal close; assess and handle or explicitly accept during this task.
  - Status 2026-08-23: implemented and verified live on Linux. Forced panic writes a crash report, shows a native dialog, exits 101; normal close exits 0 with no report; the app relaunches cleanly afterwards; release build emits nothing on stdout. XDestroyWindow segfault explicitly accepted and recorded in README (a panic hook cannot see SIGSEGV, and at M0 the app writes no user data). Windows paths and CI unverified: no remote, no Windows machine.
- [~] **M0.5 E2E smoke harness.** tauri-driver + WebDriver behavioral test asserting the app launches with window title Sublore, wired into CI. Filed from M0.1: title verification there was manual (Xvfb + xdotool), CONTRIBUTING.md §5.2 wants it automated.
  - AC: a CI job on Linux runs the E2E smoke; the test fails if the window title is not "Sublore"; removing the harness or skipping the test turns CI red.
  - AC (filed from M0.2): the same job opens `fixtures/video/sample.mkv` and fails if the native video surface is not sized over the stage element, and fails if closing the window leaves a non-zero exit status or a surviving process. M0.2's suite drives the player headless only, so `video::setup` and `VideoSurface::create` have no automated coverage today.
  - Status 2026-08-23: harness implemented in `e2e/` (tauri-driver + WebdriverIO + Mocha) with four checks: window title, fixture opened, native video surface sized over the stage, close with exit 0 and no survivors. Suite verified green locally and proven to fail when each assertion is broken. A screen smaller than the 1024x700 window makes the video checks fail, so the Xvfb size is pinned in CI. CI job never executed: no remote.
  - Harness note from M0.2: `xdotool search --name Sublore` also matches GTK's 10x10 group-leader window and lists it first, so select the toplevel by its 1024x700 geometry. Close it with an ICCCM-conformant WM_DELETE_WINDOW ClientMessage (python-xlib helper): `xdotool windowquit` proved a no-op in this WM-less Xvfb environment, and `xdotool windowclose` is XDestroyWindow, which bypasses the app's close path entirely (and currently segfaults it, see M0.4).

**Owner checklist M0:** install from CI artifact on your machine → open a video → play, pause, seek → close → reopen. Everything behaves; nothing left running in task manager.

## M1 — Subtitle formats, lossless (tasks detailed 2026-08-23; implemented and verified-by-tests 2026-08-28)

Goal: open and save SRT, ASS, VTT without destroying anything, including files from the wild.

- [x] **M1.1 Subtitle core model + SRT.** Internal document model that preserves what it does not understand (raw payloads, ordering, quirks); SRT parser + serializer; the subtitle fixture tree is born here (`fixtures/subtitles/`, committed, with a `.gitattributes` rule keeping it byte-exact).
  - AC: every clean SRT fixture round-trips parse → serialize byte-for-byte (CRLF/LF mixes, BOM, blank-line quirks, numbering gaps, overlapping cues, non-Latin scripts); every malformed fixture yields a structured error with line number and reason, never a panic, never a silent fix.
- [x] **M1.2 VTT lossless.** WEBVTT parser + serializer: header, NOTE/STYLE/REGION blocks, cue settings, voice/inline tags preserved raw.
  - AC: same byte-stable round-trip guarantee over `fixtures/subtitles/vtt/`; malformed → structured error, never a panic.
- [x] **M1.3 ASS lossless.** .ass/.ssa parser + serializer preserving section order, Format lines, styles, and override tags as raw text.
  - AC: same byte-stable round-trip guarantee over `fixtures/subtitles/ass/`; malformed → structured error, never a panic.
- [x] **M1.4 Atomic save + rolling backups.** Temp file + fsync + rename per CONTRIBUTING.md §3; before overwriting an existing subtitle file, a timestamped backup is kept (rolling cap as designed); no other code path deletes backups.
  - AC: a crash-injection behavioral test interrupts the save repeatedly and the destination is always either the old or the new content in full, never truncated or mixed; overwriting an existing file always leaves a timestamped backup; the rolling cap is enforced and tested.
- [x] **M1.5 Open/save wired into the app.** Minimal UI: open a subtitle file, show format + cue count, save-as; parse errors surface as readable UI messages.
  - AC: E2E: open an SRT fixture → format and cue count visible; save-as of a clean fixture produces a byte-identical file; open a malformed fixture → readable error message, app still alive and usable.

**Status 2026-08-28:** all five tasks implemented on `main`. Two new workspace crates: `sublore-formats` (lossless model + SRT/VTT/ASS) and `sublore-io` (atomic save, rolling backups, crash injection). 43 clean fixtures round-trip byte-for-byte and 23 malformed fixtures produce structured errors, verified independently in release profile as well as by the suites; 182 Rust tests and 7 E2E checks green. A release-only silent-corruption path found in review (the tiling guard was behind `debug_assert!`, plus a multi-byte character split that emptied the file) is fixed and covered by tests. Windows and CI unverified: no remote, no Windows machine.

**Owner checklist M1:** open a real .srt from your disk → cue count appears; save-as → the copy is byte-identical (fc/cmp); open a deliberately broken file → clear error, app keeps working.

## NOW — ahead of every functional milestone (owner ruling 2026-08-29)

Order updated 2026-08-30 by owner ruling: N2c closes the NOW block, then gate 2, then M2.0 from its first task. Decision 1 is no longer ahead of the gate — it sits inside M2.0, after the shell work it depends on, as an acceptance criterion there.

Two items jump the queue by owner decision. Full reasoning in `decisions.md`; the ruling settles all thirteen questions raised in `post-v1-plan.md`. Order is fixed: N1, then N2, then M2.0.

- [x] **N1 Close gate (decision 9).** Intercept `CloseRequested`; if anything is unsaved, ask save / discard / cancel per dirty document and honour the answer.
  - Status 2026-08-29, verified-by-tests **on Linux**: a native three-button dialog. Native rather than HTML because the video surface raises above the webview on every region update, so an HTML dialog would sit behind the video until decision 1 lands, and N1 has to stand on its own. `e2e/scripts/close-gate-check.js` drives all three answers end to end, 12 checks, and runs in CI beside the shutdown check; `shutdown-check.js` gained a fifth check proving no gate is raised over a document nobody edited. Save and discard resolve the dirty state before closing, so the close that follows finds nothing to ask and cannot loop; a save that fails closes nothing and says why. Two review passes: the first found 3 blockers, 8 serious, 9 minor; the second found 1 blocker, 5 serious, 10 minor. Owner checklist still to run.
  - Small debt, filed 2026-08-29: `close-gate-check.js` still uses fixed waits between opening the file and double-clicking a row, because without a DOM there is nothing observable there to wait on. When M2.0 gives those points something observable, replace them with `waitFor`.
  - Known gap, filed not fixed: an inline cue editor holding uncommitted text leaves the backend session clean, so the window closes without asking and that text is lost. Covering it needs the gate to consult the frontend, which is the HTML-dialog shape decision 1 will settle. Out of scope here.
  - AC: open a subtitle fixture, edit a cue, close the window: a dialog appears offering save, discard and cancel. Cancel leaves the app open with the edit still there and the file on disk untouched. Discard closes and leaves the file untouched. Save writes the edit and then closes.
  - AC: with nothing modified, closing the window exits straight away with status 0 and no dialog, and the existing shutdown checks still pass unchanged.
- [x] **N2 Video surface show and re-show (decision 2).** Build the re-show path for the native surface with a video already loaded. Prerequisite of M2.0's occlusion handling (decision 1).
  - AC: open a video, hide the surface, show it again: the frame is visible and playback continues. The assertion is on the visible frame, not on an internal flag, because mpv creates its own window inside ours and leaves it unmapped if ours is (`video/surface/mod.rs:98-99`) — a state-only assertion would pass while the user sees black.
  - **Correction, gate 2 (2026-08-30):** the automated suite does not assert the visible frame. `062f201` rewrote `video-surface.spec.js` and replaced the pixel/saturation check this AC describes with map-state and `childWindows().length > 0` checks; the file's own header says so ("What is asserted here is the surface coming back mapped with mpv still attached to it — not the pixels"). That change was never named in `062f201`'s delivery description, and CONTRIBUTING.md §5.4 requires a weakened assertion to be said out loud. The reasoning in this AC still holds — it is why the suite checks mpv's mapped child window instead of an internal flag — but the pixel half of "the frame is visible" is verified by hand only, three launches on the owner's own Wayland session showing the picture at saturation 5.86 against 2.1 for the empty shell, not by anything CI runs.
  - AC: hide and re-show ten times in a row leaves no orphan process and no leaked surface.
  - Status 2026-08-30, verified-by-tests **on Linux**: visibility is now derived, never set. One `SurfaceState` in `video/mod.rs` holds whether a video is open and whether the last reported rectangle has area, and a single `settle` turns that into show or hide; nothing else in the module touches the window. Geometry moves the surface and never decides its visibility, which is what keeps the empty stage uncovered and lets a rectangle that went empty come back. Each open carries a generation, so an older open's failure cannot clear a newer one. Two specs: `video-surface.spec.js` (playing, paused with no nudge, ten cycles) and `video-empty.spec.js` (startup, layout change, failed open), 33 E2E checks in total from 27. Pixels are measured with ffmpeg rather than ImageMagick, whose `magick` binary does not exist on the CI runner.
  - Gate 1 (2026-08-30): three review passes found 7 blockers, 15 serious, 25 minor; all fixed. The heaviest: the fix for the first pass had made the surface visible at startup with no video, and the "clock is frozen" assertion could not fail because `waitFor` returns on its first evaluation.
- [x] **N2b libmpv attaches inside a Wayland session (product defect, filed 2026-08-29).** In a Wayland session libmpv does not attach to the X11 surface it is handed: the surface reports `IsViewable` with zero children, the stage keeps its placeholder, and the transport happily reports playback over a picture that is not there. `main.rs:9` already forces `GDK_BACKEND=x11` before `gtk_init`, so GTK is not the culprit — the N2 probe's first diagnosis blamed it and was wrong. The component ignoring the `wid` is libmpv, which `player.rs:186-187` never pins to an X11 output. **This is a foundation defect on the declared primary platform:** CONTRIBUTING.md names Linux primary, and the owner's machine is Fedora in a Wayland session, so today the app only works there by accident of the environment. Runs after N2 merges and before decision 1.
  - AC: with `WAYLAND_DISPLAY` set — the owner's real session, not a cleaned one — open `fixtures/video/sample.mkv`: mpv's own window exists inside the native surface, and the surface shows a picture. Both are asserted, because the surface reports `IsViewable` either way.
  - AC: the same run leaves no orphan process and closes with status 0.
  - **CONTRIBUTING.md section 7, measured 2026-08-31 on the owner's machine** (owner ruling 4), launch and capture only, no synthetic input. Cold start to a painted window: 1095, 1076 and 460 ms armed; 1112 and 988 ms disarmed. Idle PSS sixty seconds after opening the fixture with the video paused: 142 MB armed, 148 and 221 MB disarmed. Against budgets of 2 s and 400 MB, **both are met in both configurations**, so the mitigation is accepted on cost as well as on effect.
  - Caveat on that measurement, recorded rather than smoothed: the disarmed runs are reported here as painting, while `webview-paint-check.js` measures a disarmed window as flat with a luma range of 0. The budget script detects paint with ImageMagick min/max over the captured window, which on a live compositor also catches the frame; the dedicated check uses a luma range against a measured threshold. The paint verdict of the dedicated check is the one to trust. The memory figures do not depend on the difference.
  - Note: the harness keeps clearing `WAYLAND_DISPLAY` (`e2e/lib/env.js`) for determinism, but that is hygiene, not the fix, and the comment there says so. This task's test must not use it.
  - Status 2026-08-30, verified-by-tests **on Linux**, and separately verified by running the app **on the owner's own Wayland session**: libmpv now pins `gpu-context=x11egl` whenever a `wid` is handed to it. `wayland-attach-check.js` runs with `WAYLAND_DISPLAY` set and passes the fixture as a command-line argument rather than typing it, so the input race below cannot reach it; the check discriminates, measured by deleting the option and rebuilding, which leaves the surface childless. On the real session three launches out of three showed the picture, saturation 5.86 against 2.1 for the empty shell.
  - Two defects were found on the way and both are fixed here, because the app was unusable on the primary platform without them. WebKitGTK could not allocate through DMABUF on this driver and the window opened entirely blank; `main.rs` now applies the documented escalation before the webview exists, with `SUBLORE_WEBKIT_WORKAROUNDS=0` as an escape hatch. And the workarounds cost input latency — 373 ms to reach React state against 186 ms without — which is what made `asr.spec.js` fail under Xvfb; the harness sets the escape hatch and the app keeps no knowledge of its test environment.
  - Two more were found and filed rather than fixed here: N2c, the surface misplaced under fractional scaling, and N1b, an intermittent SIGSEGV on exit after the close gate saves. Neither is caused by this change.
  - `startup_files` in `lib.rs` opens whatever is named on the command line. It exists because synthetic keystrokes go to whichever window holds the X focus, and during the N2b check on the owner's live session they landed in his own window and typed a fixture path into it.

- [x] **N2c Video surface is misplaced under fractional scaling (fixed 2026-08-30).** On the owner's display — 3840x2160 at scale 1.5 — the video plays but lands at a fraction of its rectangle, over the transcription bar instead of the stage.
  - **The mechanism, measured 2026-08-30 on that display:** the page reports `getBoundingClientRect()` in CSS pixels (`VideoStage.tsx:26-33`) and one CSS pixel here is 1.5 X pixels — `devicePixelRatio` is 1.5, measured on that display. The Linux path at the time passed those numbers through untouched (`surface/linux.rs:63-64` called `logical()`, since renamed away — see below), so the surface landed at 1/1.5 of its position and size. The first explanation, that `apply_region` mishandled a fractional `scale_factor()`, was wrong and is withdrawn.
  - **The multiplier could not come from the backend.** `window.scale_factor()` is an `AtomicI32` in `tao` and reports 1.0 on this display, so the old `physical()` would have multiplied by 1 and `logical()` by nothing. Only the page knows the ratio. That made this a change to what crosses the IPC boundary, and CONTRIBUTING.md section 6 applies: the region contract is a public interface and the Windows path moved in the same change or it would double-scale, since `scale_factor()` there does carry the ratio and the old `physical()` already multiplied by it. **Correction, gate 2:** the fix (`c7261a5`) deleted `logical()`, `physical()` and their shared `scaled()` helper from `surface/mod.rs`, replacing them with `pixels()` and `pixels_over(divisor)`; the two paragraphs above describe the pre-fix code, kept here as the diagnosis history, not the current API.
  - AC: on the owner's display at scale 1.5, the surface covers the stage. Verified by a screenshot of the app's own window, and measured: the surface's X geometry equals the stage rectangle times `devicePixelRatio`, within a pixel.
  - AC: a unit test pins the conversion, covering a fractional ratio and the 16-bit X11 clamp, and runs in CI where no fractional display exists.
  - AC: the E2E suite stays green under Xvfb, where `devicePixelRatio` is 1 and the change must be a no-op. A green suite there is necessary and not sufficient, and the delivery says so.
  - Status 2026-08-30, verified **on the owner's 1.5 display** and by tests on Linux: the page resolves the rectangle to native pixels before it crosses the IPC boundary, and each platform undoes only what it is about to redo — Linux divides by the GDK window's own scale factor because GDK re-applies it, Windows takes the numbers as they are. The factor never travels on the wire. Measured on the real display: surface 592x180 at 432,500, which is the stage's CSS rectangle times 1.5, against 395x120 before. Unit tests cover 1, 1.5 and 2 on both paths plus the 16-bit clamp; `e2e/scripts/scaled-surface-check.js` guards the 4x regression the first attempt nearly shipped and, as its own header says, does **not** prove N2c — a fractional ratio cannot be produced under Xvfb. Windows compiles in CI; the Windows path above has never been run.
  - **Correction, gate 2 (2026-08-30):** the first AC asks for a screenshot, and all the screenshot evidence ever amounted to was one sentence saying a capture of the window showed the picture inside the stage — no capture command named, no image committed to the repo, so it cannot be re-run by anyone. The measured half of that AC — the surface geometry, 592x180 at 432,500 — is independently re-checkable and was re-derived from the page's own formula during gate 2; the picture half was seen once and is not re-checkable.
  - Open suspect, measured once while diagnosing N2c and never explained: twelve seconds after launch the toplevel measured 800x600 while the startup log recorded an inner size of 1024x700. Something resized the window and nothing measured what. If the geometry work turns the cause up, it gets written down; if it does not, it stays on the record as open rather than being quietly dropped.
- [x] **N1b The window segfaults on exit after the close gate saves, under load (fixed 2026-08-30).** The main thread dies inside `_gdk_x11_display_queue_events` one loop iteration after `close_window` asked for `window.destroy()`. Two hypotheses have been measured and killed: rfd's second GTK thread (the crash survives its removal, with a core showing one GTK thread and no rfd frame) and "the sequential rate is high" (sixty sequential runs, thirty per branch, produced nothing at all).
  - **The conditions are now measured.** It does not reproduce sequentially. Under six concurrent streams it reproduces at 2 in 30 on the **save** branch and 0 in 30 on discard, same binary, same probe, load the only variable. Save and discard leave by the identical path and differ in one thing: save writes the file and its backup on the worker thread first, which delays the destroy, and load lengthens that delay. The next attempt aims there, not at "save is special".
  - No data was at risk in anything observed: the file is written and the backup kept before the crash, which lands on the way out.
  - AC: **sixty save-branch runs of `e2e/scripts/n1b-load-probe.js` in six concurrent streams, zero SIGSEGV and zero core dumps.** At the measured rate an unfixed defect survives that battery about one time in sixty. The old criterion, thirty sequential clean runs, is retired: it is now known to be unable to fail.
  - AC: thirty sequential runs of `pnpm e2e:close-gate` stay clean, and no assertion in it is weakened. It guards the behaviour even though it cannot prove the fix.
  - **Correction, gate 2 (2026-08-30):** this AC was not met by the evidence the checkbox below certifies. Sequential `pnpm e2e:close-gate` was run 30 green, 0 red on the pre-fix binary and 3 green, 0 red on the binary that shipped, so the delivered binary was run three times sequentially, not thirty, and the gap was never closed by running the remaining twenty-seven or by lowering this line to match. The number to trust for the delivered binary is three, not thirty.
  - CI: the runner is small and busy, which is the condition under which this reproduces, so the close gate check may go red there. It stays armed: it is catching a real crash.
  - Status 2026-08-30, verified-by-tests **on Linux**: `close_window` asks for the close instead of destroying the GTK window behind tao's back, and the gate's answer travels in a single-use `CLOSING` flag so the resulting close request passes without asking again. `asr` and the video surface shut down in `CloseRequested`, where every other close already does it. Judged on the delivered binary: 60 save-branch runs in six concurrent streams with 0 SIGSEGV and 0 cores, against 2 in 30 before; close gate 12/12 three times; shutdown 5/5. At the previous rate an unfixed defect survives that battery about one time in sixty, which is why this is written as "the crash did not occur in the battery built to make it occur" and not as a death certificate.
  - Two hypotheses were measured and killed on the way, and both stay on the record rather than being quietly dropped: rfd's second GTK thread, killed by a crash that survived its removal with a core showing one GTK thread and no rfd frame, and a high sequential rate, killed by sixty sequential runs that produced nothing at all.
- [x] **N1c The file picker still starts rfd's second GTK thread (latent defect, filed 2026-08-30).** Not the cause of N1b — that hypothesis is refuted — but unsound on its own terms: rfd spawns a permanent thread that iterates GTK for the rest of the process's life, and GTK3 is not built to be driven from two threads. N1b removed the close gate's three message dialogs from that path; `project::choose_path` (`project/mod.rs:245-257`) still calls `blocking_pick_folder` and `blocking_pick_file` through the plugin, so choosing a project path arms the same race for the rest of the session. `crash/mod.rs:187` also uses the plugin, deliberately: it runs when the main loop may already be gone, which is exactly the case a GTK dialog on the main thread cannot serve. Upstream is no help — rfd 0.17.2 has the same design, and its stop flag is a second `Arc` the thread never sees, so the loop cannot be stopped at all.
  - AC: after choosing a project folder and a project file, `/proc/self/task` holds no thread running `gtk_main_iteration` other than the main one, asserted by a check that fails when the plugin path is restored.
  - AC: the picker still returns a cancelled choice as a cancellation, proved by `pnpm e2e:picker-thread`.
  - **The "opens at the last used directory" clause was struck, 2026-08-31.** It described a behaviour that never existed: nothing here stored a directory, rfd never called `set_current_folder`, and GTK 3.24's `last-folder-uri` is a dead key. The chooser opened on GTK's Recent list before the change and opens on Recent after it. Making the picker remember is new behaviour with its own check, filed as N7 rather than smuggled in under a criterion that read as preservation.

- [x] **N7 The picker does not remember where you were (minor, filed 2026-08-31).** Every project path is chosen from GTK's Recent list, so a translator working through a series picks their way back to the same folder each time. Split out of N1c, whose second criterion asked to preserve a memory that had never been implemented.
  - AC: choosing a folder, closing the app and choosing again opens the chooser at the folder chosen last, proved by a check that fails when the stored path is ignored.
  - AC: a remembered folder that no longer exists opens the chooser at its default rather than failing.
  - AC: nothing is remembered from a cancelled chooser.
  - Status 2026-09-01, verified-by-tests **on Linux**: each of the five chooser kinds remembers its own folder, one per kind rather than one for all — a translator who has just attached a subtitle and then opens a video is not in the same folder. The folders live in `chooser-folders.json` in the app data directory, a convenience and not the user's data, so nothing of this goes near a project folder (decision 20). Only a chosen path is written, so a cancelled chooser leaves the memory as it was, and a remembered folder that is no longer a directory is dropped before the chooser is built. `pnpm e2e:picker-thread` grew from 9 checks to 14: a second run of the app over the same data home opens the folder chooser and accepts it without naming anything, which a `SelectFolder` chooser answers with the folder it is showing. Measured by deleting the `set_current_folder` call and rebuilding: the check stops at that step with exit 1 and the chooser still on screen, because GTK's Recent list has an insensitive accept button, and passes 14/14 with the call restored.

- [x] **N4 Launching without a display panics instead of explaining (minor, filed 2026-08-30).** `sublore` with no `DISPLAY` and no `WAYLAND_DISPLAY` — over SSH, in a container, on a display that went away — dies with a raw library panic from `tao-0.35.3/src/platform_impl/linux/event_loop.rs:217`, `Failed to initialize gtk backend!`, plus a `RUST_BACKTRACE` note. Intelligible to a developer and useless to anyone else, and CONTRIBUTING.md section 6 asks for actionable messages at boundaries. Pre-existing, not introduced by the gate 2 range, and found while reproducing a harness artefact rather than by looking for it.
  - AC: with no display reachable, the app exits non-zero having printed one plain line naming the cause and what to do, and no panic trace.
  - AC: a check proves it, and fails if the message goes back to being a panic.
  - Status 2026-09-01, verified-by-tests **on Linux**: `main.rs` refuses a launch whose `DISPLAY` is unset or empty, printing one line and exiting 1 before GTK is touched. `pnpm e2e:no-display` asserts the status, the line, the absence of a panic trace and the absence of a crash report; it is the only check that runs without an X server, so it has its own CI step rather than going through `e2e-check.sh`, which supplies one. Measured by deleting the guard and rebuilding: the check stops at its first assertion with exit 101 and the tao panic back on stderr, then passes again with the guard restored.
  - Left alone deliberately: a `DISPLAY` that names a server nobody is running still panics inside GTK. Proving it is missing means opening a connection, which is a different guard from reading an environment variable, and this AC is about the launch that has no display at all.

- [x] **M3.6 The third route cannot save a document with no file either (filed 2026-09-01).** B2 gave the toolbar's Save and the close gate's Save a way to write a document that has never had a file: they ask for a path. `adopt_answered`, the route where one transcription replaces another, was not given one, so answering Save there still fails with `noPath`. The replacement does not happen and the old document is kept, which is the safe direction and is what `a_save_that_cannot_be_written_replaces_nothing` pins, but it is now the only route that behaves differently from the other two.
  - AC: transcribing over an unsaved transcription and answering Save raises the same first-save chooser the toolbar raises, writes there, and then takes the new cues.
  - AC: cancelling that chooser writes nothing, replaces nothing, and leaves the cues that were there.
  - AC: E2E drives it, and the test that pins the failed-save path keeps its assertion.
  - Status 2026-09-01, verified-by-tests **on Linux**: the third route asks now. `subtitle_adopt_transcription` intercepts the `noPath` its own Save answer raises and puts up `SubtitleFirstSave`, the same chooser the toolbar's Save and the close gate's Save put up; `adopt_answered_at` writes there and replaces under one lock, so a write that fails still replaces nothing. Dismissing that chooser is a cancellation and not a failure, exactly as it is at the gate: nothing written, nothing replaced, the cues left where they were. A chooser that could not be raised stays a failed save. `a_save_that_cannot_be_written_replaces_nothing` keeps its assertion unchanged and still describes a state that happens: it drives the body, and the body still refuses a save with nowhere to write; what is new is the command layer above it asking where. Two E2E tests added to `asr.spec.js`, `EXPECTED_TESTS` 47 to 49, and three Rust tests over the new body. The 49 E2E tests, the close-gate check and the whole Rust suite are green under Xvfb here; Windows compiles under `cargo clippy --target x86_64-pc-windows-gnu` and is unverified.

- [ ] **M3.7 `asr.spec.js` passes state down a chain of nine tests (filed 2026-09-01).** On CI run 33537893939 the four first-save tests failed together, the first of them on `timed out after 20000ms waiting for a toplevel named "Save the subtitle"`. Its own precondition assertion — the document is dirty — passed, so the document was dirty and **had a file**, which makes Save write in place and the chooser it waits for never appear. Main is green with these tests and they have passed three CI runs, so this is intermittent, and the mechanism above is suspected from the log rather than proved.
  - The shape of the problem is not in doubt: each test in that file inherits whatever the eight before it left, and whether the open document has a path by the time the first-save tests run depends on a transcription being adopted several tests earlier.
  - AC: each first-save test establishes the state it needs rather than inheriting it, and asserts that state before acting — a document with no file is asserted to have none.
  - AC: the file is run ten times in a row on Linux with no failure, and the number is recorded.
  - AC: no assertion is weakened; a test that cannot establish its own precondition says so and is restructured, not relaxed.

- [x] **The document is drawn on the video frame (2026-09-03).** Decision 7's first half, landed ahead of M2.6 because the owner found it missing and nothing about timing works without seeing the line on the picture. mpv reads a shadow copy of the open document from the app's own cache directory: the user's subtitle file is never what mpv reads, is never written to produce a preview, and no backup is made for one. Every route that changes what should be on screen ends at one refresh, so opening the document first and opening the video first are the same path and neither is a special case.
  - It asks mpv what it holds rather than remembering: the external track list is read back after every change, so a rewrite re-reads the track already loaded instead of stacking a new one per edit, and the media's own embedded subtitle tracks are never touched.
  - **What the check asserts, and what it cannot.** The harness cannot read the picture, for the reason `video-surface.spec.js` gives about Xvfb and llvmpipe. It asserts mpv's own answer instead, which the app reads back and logs: how many external tracks there are, whether ours is the selected one, whether it is visible, and how many characters long the line at the playhead is. That is one step short of the glyphs. **Nobody has seen the words on the frame with their own eyes; the owner's checklist is where that is answered.**
  - Five tests, and all five were shown to go red. Making every refresh add a track instead of re-reading turns three of them red; writing the shadow but never telling mpv turns four red; making the preview save the user's document turns exactly one red, and it is the one that guards the file. `EXPECTED_TESTS` 121 to 126.
  - Found by reading, after the tests were written and before the commit: `show_subtitles` promised to make its path the one external track and did not remove the others, so changing the document's format while a video stayed loaded would leave the old shadow's track behind. Fixed by dropping every external track that is not the wanted one. **That path has no test**: it needs an ASS document and an SRT one opened in turn against one video, and the fixtures for it do not exist yet.
  - **What the owner has to look at with his own eyes, and why it is a specific thing rather than "check it works".** The check reads mpv's `sub-text` property, and mpv is documented to update that property while leaving a paused still frame unredrawn: mpv issues 12130 (subtitles do not appear when switching tracks while paused) and 11936 (OSD and subtitles not refreshed during a still frame) are both open and both sit exactly in the gap between what the check can see and what the user sees. The player opens every file paused at zero (`src-tauri/src/video/player.rs:339`), so this is the normal case and not an edge one. **Owner checklist item: open a video, pause on a cue, edit that cue's text, and look at the frame without touching anything else. The words on the picture have to change.**
  - Also read on the way, and sidestepped rather than fixed: mpv issue 8231 reports `sub-reload` failing after a `sub-remove` of the same track. The code never does that pair, because a document that was dropped comes back through `sub-add`, not through a reload. It does do `sub-remove` of a stale track followed by `sub-reload` of a different one, in the same call, when the document changes format. That sequence has no test either, for the same reason the format switch has none.
  - Also unfixed, and small: the character count at the playhead reads `sub-text` and treats every failure as "no line", so an mpv error there would read as absence. It weakens the instrument, it does not weaken the feature.

- [x] **M2.4 W10 The waveform's two numbers, measured (2026-09-03).** `pnpm e2e:waveform-budget` opens a fixture twice against one data home and reads the app's own account of both runs: when it first handed peaks over, and how long the job took altogether. The first number is guarded in CI on every push; the second belongs to the owner's machine, because a runner is not the machine CONTRIBUTING.md section 7 names and a 24-minute fixture is not a thing to generate on every push.
  - **Measured on the owner's machine (AMD Ryzen 7 7700, 16 threads, Linux), 2026-09-03.** The 60 s fixture: first peaks 171 ms cold and 54 ms warm, read all the way in 355 ms cold and 72 ms warm. The 24-minute fixture: first peaks 162 ms cold and 79 ms warm, read all the way in **4436 ms** cold and 512 ms warm. The budgets are 2000 ms and 20000 ms. Verified on Linux.
  - The stopwatch stops where the peaks are handed over, not where they are produced. The first version stopped at production, and a build that buffered every chunk and delivered them all at the end still passed it, which is the one regression this budget exists to catch. With the stopwatch moved, that same build reports its first peaks and its finish at the same millisecond and the check fails.
  - It also answers the risk the cache wiring left open: replaying a 24-minute file hands over about 1440 chunks with nothing pacing them, and that replay takes 512 ms altogether. It is not a problem.

- [ ] **N14 The waveform panel sometimes never appears in CI, and the timeout says nothing about why (filed 2026-09-03).** `waveform.spec.js` fails on `timed out after 30000ms waiting for the waveform panel to appear and hold ink`, and the test after it then runs out mocha's own clock. The panel is gated on the first peak arriving, so that one sentence covers a job that never started, a job that failed, a video that never opened and a canvas that is empty, and it names none of them.
  - Not a spec that has never worked: green in CI on #59 at 3.2s, and green here 3 runs out of 3 at about 3.4s each.
  - Not the D-Bus portal. The first red run was the one that took the portal away, which made it the obvious suspect, and #63 refutes it: portal present, 12m02s of smoke, waveform red all the same.
  - Not the fixtures. `make-waveform-fixtures.sh` runs in the e2e job and the job log holds all three files with their probed streams; `requireWaveformFixture()` would refuse the run outright if one were missing, and the spec's first test passes.
  - Not concurrency between runners: `maxInstances` is 1, so the fourteen spec files run one after another in the green run and in the red ones alike.
  - Not the fourteenth spec on a warm machine either: the whole suite in order here is 14 files, 93 tests, green, with the waveform 4 for 4 at 3.2s in its usual last position.
  - **What the diagnostic says, run 33704913953.** `the video is open, 0 of 60s; no .waveform__canvas (panel absent); no failure is on the status bar.` So mpv has the file and knows its length, the panel never appeared, and no `audio://error` ever arrived. The job produced neither peaks nor a failure, which leaves the two branches in `start_for_playing_track` that return before `start_job` and tell only the log: mpv refusing to list the tracks, and no track in the list carrying `selected`.
  - **Reproduced 2026-09-03, and it is the second branch.** `playing` is mpv's `track-list/N/selected`, read on the thread that has just loaded the file, and W5 had already found the same shape one field over: asking mpv for the path right after a successful open answered `None` on a loaded runner, and that branch returned in silence too. Forcing `selected` to `false` in `player.rs` and running the spec against the code as it was reproduces CI down to its shape: 2 passing, 2 failing, 1m 33s, the same diagnostic sentence, and the app's own log saying `has no playing audio track, so nothing is peaked; mpv listed 1 audio tracks: stream 1 selected=false`. With the same forcing and `track_to_peak` in place: 4 passing in 3.3s, and the log says it fell back and to which stream.
  - So it is a product defect and not a test one. On a machine slow enough, a video that carries audio opened with no waveform at all, and the only thing that said so was a log line CI was throwing away.
  - Closed by `track_to_peak`: the track mpv is playing, or the first audio track when mpv has marked none. A file with no audio still spawns no child, which is decision 24 E3 and is the empty list rather than the fallback.
  - Left open on purpose: **why** mpv has not marked a track by `file-loaded` on a loaded runner. The fallback makes the panel right either way and says out loud when it was needed, so the runs still carry the evidence. AC for closing this entry: that line stops appearing in CI logs, or its cause is named.
- [x] **N21 The timing keyboard is the numeric keypad, and nothing here can dispatch it yet (filed 2026-09-04, CLOSED 2026-09-04).** Read off the reference's own default bindings rather than guessed: commit is KP_Enter, play selection KP_5, play before KP_1, play after KP_3, stop KP_8, shorten KP_7. The whole timing workflow sits on the numpad, one hand, no modifiers. That is the layout the work is done in and it is worth having.
  - **Sublore cannot dispatch it today, and the reason is not the parser.** `acceleratorFor` now reads the registry, but it requires Ctrl, and a bare key needs to know first whether it belongs to a text field or to the shell. That question is answered in `CueList.tsx` by `ownsTheKeyboard`, which also owns the flush of a pending cue editor. A modifier-free layer means moving both, or giving the shell its own copy, and a second copy of that rule is how the two drift.
  - **Also unresolved:** a numpad key arrives as `event.code` (`Numpad1`), not as `event.key`, because `event.key` is `"1"` or `"End"` depending on Num Lock. Any binding table has to say which of the two it matches on, and the accelerator strings today carry neither.
  - **Until it exists** the four nudge commands and the four playback commands are menu items with no key, which is the slow way to do the fast part of the job. They work; they are not yet worth using.
  - **What it needs:** one keyboard context that both the shell and the grid read, holding the flush and the text-field rule once. That is a task of its own and it is the one that makes every timing command actually usable.
  - **CLOSED, and the entry above was wrong about what the task was.** The keypad is not the window context's to dispatch: interface-spec 10.9 already refuses the always-on numpad layer, on the grounds that every command in it has a waveform-context binding anyway. What the window context did owe, and had owed since the spec was written, is 10.4's Ctrl+digit bindings. Four of those name commands that exist, and they landed. The keypad now belongs to the waveform context, which needs a focused-panel layer that does not exist yet.
  - **What actually moved.** One listener, in `src/keyboard.ts`, read by the shell. `CueList.tsx` lost its own window listener, three props and the field rule, because the reason it held them was wrong: every command in `App.tsx` already flushed both editors, so the grid's copy was a narrower flush in front of a proper one, plus a route around the availability gate. Ctrl+Z, Ctrl+Y and Ctrl+S now run through the gate a click runs through, which is the last route that did not.
  - **Measured, not assumed, before any of it was designed.** A keypad key's `code` is stable across Num Lock and its `key` is not. `Ctrl+Shift+1` delivers `key: "!"`, so a digit cannot be matched on `key` at all. Both readings are in docs/keyboard-tasks.md with the probe's numbers.
  - **A blocker for whoever builds the waveform context, found here and worth having early:** interface-spec 10.6 gives it `Numpad +` and `Numpad −`, and under xdotool on this keymap both arrive with `altKey: true`, preceded by a real `AltLeft` press, in both Num Lock states. Any dispatcher that excludes Alt will never see them, so those two bindings cannot be checked through this harness as it stands. `Numpad5` and `NumpadEnter` arrive clean.

  - **One code path of this is closed (2026-09-05).** `waveform-tracks.spec.js` "draws the quarter-scale track after switching to it" went red on the runner with the menu marking track one while the panel drew track two. Not a race and not a regression: the code was byte identical across a passing run and a failing one. The two answers were computed in two places from two different rules. The panel drew the stream chosen by the requested id (`audio/mod.rs:358`), and the mark came from mpv's own `selected` flag read one statement after the write, folded in the page as `tracks.find((t) => t.playing) ?? tracks[0]` (`useAudioTracks.ts:72`). Where mpv marks nothing, both stale readings land on the first track.
  - **The fix gives the question one owner.** Both audio commands now answer `AudioTrackList { tracks, currentId }`, where `currentId` is the track the panel is drawing: `track_to_peak`'s choice on the open path, the id that was asked for on the switch path. `AudioTrack.playing` still means mpv's flag and nothing else, so decision 24 E2 is not overruled, and a switch mpv has not marked now writes a log line naming every stream and what mpv said about it. The open question this entry exists for, why mpv marks no track on a loaded runner, is untouched.
  - **The owner call this leaves.** E2 says mpv is the authority on which track is playing. Answering the switch from the id that was requested is the app overruling mpv on that one path, and no written rule says whether E2 governs only the open path, where the app has no other information, or the switch path too, where the app is the one that chose.
- [ ] **N24 The E2E battery is serial for a reason that can be removed, and it costs six and a half minutes a gate (filed 2026-09-04).** `wdio.conf.js` sets `maxInstances: 1`, so twenty-seven spec files run one after another. The owner freed the machine for heavy work on 2026-09-04, so the ceiling is no longer thermal: it is that every worker would share one `SUBLORE_E2E_DATA_HOME`, and the app reopens whichever project it last had, which is N19's mechanism turned into a collision between live sessions rather than between neighbours in time.
  - **What it needs:** a data home per worker rather than per run. `wdio.conf.js:19` makes one in the launcher and every worker inherits it; a `beforeSession` could give each its own directory under it. The stub sidecar and the model directory are read only and stay shared, so only the project state has to split.
  - **What it would buy:** the gate is the loop's slowest step by a wide margin, and three or four workers would take it under two minutes. Every block in this session paid it at least twice, once for the mutations and once for the final run.
  - **What to watch:** the machine stops initialising GTK after about four `xvfb-run` launches in quick succession, measured on 2026-09-03 under the old thermal ceiling. Whether that was resource exhaustion that a healthy machine survives is unknown and is the first thing to measure, because it is exactly what parallel workers would do.
  - Not attempted yet. Filed rather than half done, and it is infrastructure rather than product, so it waits behind the feature it would speed up.

- [x] **N8f The vocabulary check in CI (done 2026-09-04, verified on Linux).** `scripts/check-vocabulary.sh`, gating on its own job in 0.077s. The product vocabulary of the closed module stays out of this repository and so does the reference editor's name, both of which were grepped by hand until now.
  - **The rule per word is the work, not the grep.** `pro` needs three match shapes to exist at all, because `project`, `process`, `property`, `produce` and `provide` are everywhere here: standalone or snake_case, an inner camelCase segment, and a leading lowercase segment, so `openTermbaseEditor` is a hit and `PROMPT` is one word rather than one.
  - **`term` alone is deliberately not checked, and the reason is in the script.** It matches ordinary English including GPL-3.0's own boilerplate in this repository's LICENSE, and nothing short of a parser separates that from a leak. `termbase` covers the concept a leak would carry.
  - **Its first run against real code found three leaks**, all in prose written on this branch: a comment in the storage guard, `sublore-matcher`'s package description, and the fixture module's header. The check is stricter than the letter of the rule, which names two files and identifiers, and that is deliberate: a word that reaches a comment reaches an identifier next.
  - The name is base64-encoded in the script and decoded at run time, so grepping the check itself for the name still finds nothing. Both classes were proved red then green with a planted violation.

- [ ] **N8e The rest of the host table.** Four of the seventeen slots are filled. `document`, `cue_at`, `log` and `find` work; `propose`, `for_each_line`, `db_run`, `db_transaction`, the three panel calls, `status`, `progress`, `should_cancel` and the wiring that makes `invoke` reach a module are not built. Criteria are in the planning archive as `docs/module-host-tasks.md`, H5 to H8.
  - **Done 2026-09-04, verified on Linux: H1 to H4.** The gate arms a record naming the calling thread before every call into a module and disarms it after, so a module calling from a thread of its own gets an error code rather than a deadlock. `log` puts a module's line in the app's own log under its file name, capped and validated. `find` hands over `sublore-matcher`, so the free product's find and a module's quality pass cannot drift apart. `document` and `cue_at` are served out of the session.
  - **The session lock is held across the whole module call, and that is memory safety rather than economy.** `cue_at` hands over text borrowed out of the session, and the thing that can drop it is not the module: it is the user, committing an edit from the window's own thread while the call is in flight. The cost of the alternative was measured first and was irrelevant, which is what the measurement was for: 3.9 ns per uncontended lock, 7.84 microseconds over two thousand cues.
  - **Section 2.5 asks for a generation number that cannot be a check.** A generation only checks anything if the caller presents one, and a module holds the host pointer and no token. Both things it was written for are caught without it.
  - **A prediction about normalized form was wrong.** An ASS line break arrives as the two characters `\N`, not as a newline: normalization collapses `\r\n` and nothing else. The check that found it was also proving the wrong thing, asserting the CRLF rule on an ASS fixture where a carriage return inside a cue cannot exist.
  - **Clippy caught an aliasing hazard a written safety argument had waved at.** Handing the session out as `&mut` from a `&self` can be done twice, and a comment saying no callback keeps it is not a constraint. The accessor returns the raw pointer now and each reader makes its own shared reference at the point of use.
  - **The closed module loads through this, for real.** A cdylib built in a separate private repository, linking only `sublore-module-api`, was put beside the binary: the app created it, the module called `log` and `find` through the table, the core's comparison answered, and it contributed four items.

- [x] **N8d Migration 2 and the module authorizer (done 2026-09-04, verified-by-tests on Linux).** `module_schema` is in, `CURRENT_VERSION` is 2, and `crates/sublore-project/src/module_guard.rs` holds a module to its own tables through SQLite's authorizer. Nine behavioural checks against a real migrated database, plus two unit checks on the id and the prefix. Both mutations the criteria named turned exactly the right checks red.
  - **`user_version` is the core's forever, and that is now a guard rather than a promise.** The authorizer denies `Pragma`, which is what stops a module recording its own ladder in the SQLite header and making the next free core refuse the whole project.
  - **The bookkeeping a schema change emits had to be permitted**, measured rather than guessed: creating a table emits an insert, five updates and a read on `sqlite_master`. That is safe only because `writable_schema` is a pragma and pragmas are denied; both halves have their own check.
  - **A check that used a table with no index proved nothing.** `REINDEX series` emits no authorizer action at all and so succeeded; `REINDEX episodes` emits one. Written down because the same shape will catch the next person.
  - The `hooks` feature of rusqlite is now on. It is `hooks = []` in rusqlite's own manifest, so it pulls no new dependency and only compiles more of a crate the workspace already builds.

- [ ] **N26 `picker-thread` has been broken since the rail was rebuilt, and nothing could tell us (filed 2026-09-04).** The check guards a real platform invariant, that choosing a path starts no second GTK thread, and it drives the rail by focus walk and menu position because it deliberately has no DOM. The rail became a tree with a menu built from the command registry, and the check's map of it went stale. It runs in CI's `e2e smoke` job, which this branch reached for the first time tonight, so the breakage is as old as the rebuild.
  - **One defect found and fixed, verified locally.** `ADD_EPISODE` was 0, from a rail that offered a different menu. The project node now draws create, open, add episode, close, delete in that fixed order, so it is 2. The check pressed Open project and then waited for a write a chooser was never going to make, which is why its message said "Add episode wrote nothing" and was both true and misleading. The indexes are stable only because of the 2026-09-03 ruling that commands grey and never vanish, and the comment says so.
  - **Three hypotheses eliminated by measurement rather than by argument.** The rail's focus order is unchanged where the check walks: the anchor at 52,80 is still `H2.rail__cap`, which is not focusable, and one tab still reaches the project node. The `Menu` key arrives as `ContextMenu` with no modifier, so it is not the class of fault that made `xdotool key F3` press Alt. The chooser titles the check waits for are the ones `strings.rs` holds.
  - **What is left, and it is new rather than old.** With the index fixed the check gets one assertion further and then finds no chooser after opening the episode node's menu. It now opens a menu and confirms a name dialog where it used to open a chooser, so what it leaves behind differs, and the window list at the failure still held a 346x166 popup. The next step is to find what has focus after Add episode is confirmed, not to change another constant.
  - **A fourth hypothesis, tried and refused.** `RailMenu` draws a backdrop that swallows the click dismissing it, so a menu left open from an earlier attempt would eat the click that re-anchors the walk and no retry could recover. Pressing Escape before each re-anchor changed nothing: the check stops at the same assertion, on this machine and on the runner alike. The 346x166 window in the failure's window list is therefore not what is in the way.
  - **Both paragraphs above are wrong, and the correction is the finding (2026-09-05).** `ADD_EPISODE = 2` is a position and the check spends it as a count of Down presses, and `RailMenu` puts the cursor on the first item that *can run*, not on the first item (`src/components/RailMenu.tsx:37,45`), while its arrow walk wraps and steps over greyed items (`:82-86`). With a project open, Create project is greyed (`ProjectRail.tsx:95`), so the menu opens on index 1 and two Downs land on index 3, **Close project**. The 2026-09-03 ruling fixes the item list; it says nothing about where the cursor starts, and that is the half the comment assumed.
  - **The assertion behind it could not tell, and that is the worse half.** The episode was proved by the size and mtime of `project.sublore-wal` changing. A clean close of a WAL database checkpoints and removes that file, which is a change, so the check printed `ok` on a project it had just closed. Every walk after that ran against an empty rail. This is `e2e/README.md`'s own rule about a green run that proves nothing, met in the field.
  - **The 346x166 window was not in the way either.** It is the native video surface, defined in `video-empty.spec.js:52-58` as the toplevel's one child over 50x50, unmapped until a video is ready and receiving no events. No probe was needed to identify it, only reading.
  - **What was actually needed was observability in the app, not an instrument in the check.** `src-tauri/src/project/mod.rs` now writes one line per mutating command, after it succeeds, naming what ran and the title or folder it ran with. The check asserts on those lines instead of on side effects, so a walk that presses the wrong item says which one it pressed. The Down count is now `(item - from + items) % items` with the cursor's start stated, and every gesture is followed by an assertion naming the command that actually ran.
  - **Green on Linux, 20 of 20 checks, 2026-09-05.** The count rose from 15 because the walk's starting state is now asserted rather than assumed, at both launches.
  - **One owner decision is left, and it is the durable fix.** `RailMenu` claims `role="menu"` and offers no Home or End, so any keyboard walk can only ever be a distance from a cursor it cannot see. Home would make the menu index absolute and retire the harness's model of the greying entirely. Against it: `docs/shell-layout.md`'s dropdown table names Alt, Left/Right, Up/Down, Enter and Escape and nothing else, and it rules out Alt mnemonics as i18n work out of proportion. The alternative is to make `RailMenu`'s cursor start on item 0 whatever its greying, matching its own arrow walk and the 2026-09-03 ruling's spirit, at the cost of parting company with `MenuBar`, whose Alt behaviour that table pins to the first enabled item.

- [ ] **N25 Nine e2e checks are red in CI and green here, and tonight was the first time CI ran most of them (filed 2026-09-04).** `ui-rebuild` was pushed for the first time with pull request 78, so the specs written on it had never met a runner. The battery is 29 of 29 on the owner's machine and the `e2e smoke (ubuntu)` job reports nine failures. Three of the four spec files involved do not exist on `main`, so this is not a regression against a green baseline: it is a first contact.
  - **`interface-scale.spec.js`, six checks, and the first one is settled by measurement rather than left as two readings.** At 150 per cent in a 1024x700 window the runner reports `button.toolbar__button toolbar__edit-redo spans 963..1043 across`, nineteen pixels past the edge. Measured here with a throwaway probe on 2026-09-04, same window: the rightmost button ends at 628 at 100 per cent, 697 at 110, and **937 at 150, leaving 87 pixels of room**. The runner's toolbar is 106 pixels wider than this machine's, about eleven per cent, so it fits here and does not fit there.
    - **The conclusion is neither reading.** The toolbar is not too wide by design and the check is not anchored to a number someone picked: the layout simply has **no reserve at 150 per cent**, and eighty-seven pixels out of a thousand is inside the range ordinary font differences cover. CI is right and this machine is the lucky one. A user whose fonts are a little wider than the owner's sees Redo clipped at 150 per cent.
    - **What it needs is a design decision, not a mechanical fix.** Dropping buttons at large scales is out under the 2026-09-03 ruling that commands grey and never vanish, so the answers are a toolbar that wraps or scrolls when it runs out of room, or a minimum window width that accounts for the largest scale. Owner call.
  - Also there: `draws the type half again as tall at 150 per cent` reports `menuTitle` with `follows: false`, so the menu bar's type does not scale with the rest.
  - **`dividers.spec.js`, one check, and reading it turns the whole entry into one finding rather than three.** The assertion that fails is not about the edge at all: at the video panel's floor the transport must still be one row, `expect(boxOf(".controls").height).toBeLessThan(60)`, and the runner reports 76.17, so it has wrapped to two. The spec's own comment says where the number came from: "the floor was measured as the width that keeps it on one row". Measured on this machine, once.
    - **So `MIN_VIDEO_WIDTH` and the toolbar at 150 per cent are the same defect in two places**: a layout minimum derived from what fitted on the owner's fonts, with no reserve for fonts a little wider. Eleven per cent is enough to break both. The decision is therefore one decision and not two, and it is not "add nineteen pixels here and a few there": it is whether a layout minimum may be a number someone measured, or has to be derived from the content at run time.
  - **`asr.spec.js`, two checks, and this is the only one that could be a regression.** It exists on `main`, whose CI was green on 2026-09-03. A transcription that finishes while unsaved work is open must ask where the work goes, and cancelling that chooser must replace nothing; on the runner the document holds the transcribed cues afterwards instead. It passes here every time. Either something between `main` and this branch changed that path, or the runner is slow enough to reorder the chooser and the run finishing.
    - **The obvious suspect is eliminated, by reading rather than by guessing.** This branch does touch `subtitle_adopt_transcription`, which is what the check drives, and the change is an extraction of the body into `adopt_through_dialogs` so that one `preview::refresh` covers every way out of it. The cancel branch is unchanged, comment included: nothing is written, nothing is replaced, the cues stay. So the adoption logic is not what moved.
    - **What is left is a race in the check's own setup.** The work in the way is typed and committed on a field commit; a transcription that finishes before that commit lands leaves the document clean, and a clean document is replaced without anyone being asked. That is consistent with passing here every time and failing on a slower runner, and it is a check that establishes its state rather than waiting for it. Not proved: it needs the runner, and one run is one data point.
    - **The second run gave the second data point, and it points the same way.** On the CI run of 2026-09-04 for `98eba39`, with nothing about the adoption path changed between the two, both `asr` checks passed. The same two checks had failed on the run before it. A failure that comes and goes on the same code is a race, not a regression, so the bisect this entry asked for is not what settles it: what would is the check waiting for the field commit to land rather than assuming it has.
  - **What is already fixed and is not part of this**: the smoke job never built the module fixture example targets, so `modules.spec.js` could not start at all. That is a step now.
  - **Resolved on 2026-09-05, and the entry's own conclusion needed correcting first.** It read as one defect in several places. It is three, and the seven checks split five, one and one.
    - **Five are the transport wrapping, and they are fixed.** `.controls` is a wrapping flex row whose one-row width is padding, gaps and three inflexible children, every one of them a width the machine's fonts decide. `MIN_VIDEO_WIDTH = 220` had **two pixels of margin at 90 per cent and four at 150** by its own comment, against fonts eleven per cent wider on the runner. The floor is no longer a number: `src/measure.ts` measures what the row asks for on one line, off the row itself, before the paint and again when `document.fonts.ready` settles. `.controls__slider` gains `min-width: 2.5rem`, which is what makes the width the sum computes and the width the row wraps at the same number rather than two numbers that happen to agree.
    - **Proved by mutation on the owner's machine, which is what the old checks could not do.** Making a flexible child contribute nothing to the floor reddens `dividers` "stops the video edge at a floor" and four `interface-scale` checks, **with the same heights the runner had printed, 76.171875 and 63.75**. The checks now reproduce CI's failure locally when the derivation is broken.
    - **One was the check's own fault.** `draws the type half again as tall at 150 per cent` asserted `grew > 1.4 && grew <= 1.5` on the whole box height, which forbids any box from rounding up, and `.menubar__title` has no border to lose the rounding into. It now reads the content box against 1.5 within the line box's own rounding, and asserts separately that the padding grew by exactly 1.5, which is the half with nothing to hide in.
    - **The last one was the owner's and he answered it on 2026-09-05: a minimum window width.** The toolbar at 150 per cent in a 1024 px window is about 1038 px on a wide font and no arrangement of the seven current labels fits, so with commands greying rather than vanishing the only answers were to wrap it, to scroll it, to shorten the labels, or to stop the window being that narrow. He chose the last.
    - **It is measured, not declared, for the same reason the panel floor is.** `src/hooks/useWindowFloor.ts` reads the widest of `.menubar`, `.toolbar` and `.cuelist__head`, the three rows that cannot be drawn narrower than what is in them, takes the wider of that and what the block under them asks for, and hands it to `layout_set_minimum_width`, which calls `set_min_size` and brings a window already under it up once. The static `minWidth` in `tauri.conf.json` stays as the floor before anything has been measured. The shell publishes the number as `data-minimum-width`, which is what the check reads, because there is no number it could be compared against.
    - **The consequence, stated rather than hidden.** S1's criterion names 1024x700 and at 150 per cent the window can no longer be that narrow. The check now asserts it at the narrowest window that exists at each size, and then asks for one pixel under the floor and requires the window to come back to exactly the floor, which is what says the floor is a floor and not a wide enough guess. Removing the `set_min_size` call reddens it. Verified on Linux, 2026-09-05.

- [ ] **N23 Replace all cannot be one undo step, and the reason is in the history rather than in the feature (filed 2026-09-04).** Found while scoping find and replace, before any of it was built. `History::merge_target` joins two edits only when the second replaces exactly the bytes the first wrote, at the same offset, inside a one second window (`crates/sublore-edit/src/history.rs:191-206`). That is typing coalescing on one field. A replace across forty cues is forty splices at forty offsets, so it would be forty undo steps, and undoing a replace all one cue at a time is not undoing it.
  - **The shape to build,** of the two available: one splice spanning from the first changed byte to the last, carrying the unchanged bytes between them, because an `Entry` holds one `Splice` and every replay path, inverse, weight and verification already handles exactly that. The alternative, an entry holding several splices, teaches all of them to loop.
  - **Its cost is measured, not guessed.** `weight()` is removed plus inserted against an 8 MB budget over at most 200 entries (`history.rs:19-21`). The largest fixture is 2000 cues in 131 KB, so a replace all touching its first and last cue weighs about 262 KB, and thirty of those fit before the bound evicts oldest first. One episode is a fraction of that.
  - **Criteria written before any code, in docs/find-replace-tasks.md,** as F1. F2 is the search, which needs no new IPC because the frontend already holds every cue's text. F3 is the window, and it carries its own foundation: F3 the key is neither a letter nor a digit, so it is the first accelerator with no Ctrl in it, and it is where the modifier-free half of the field rule gets built.

- [ ] **N22 Two fixtures carry the reference editor's name in their own bytes, and taking it out would cost a data-safety proof (filed 2026-09-04).** The permanent rule is that the name never appears in this repository, and the grep before this commit found eight occurrences. Five were dealt with: one was a sentence we wrote into a fixture's dialogue text and was rewritten, and three sat in `.omc/plans/`, which is local agent scratch that was never tracked and is now in `.gitignore` so it cannot be committed by accident.
  - **The three that remain are data, not authorship.** `unknown-sections.ass` carries the reference editor's own project-garbage section, whose header is that editor's name followed by two words, holding `Audio File:`, `Video File:`, `Video AR Value:` and `Active Line:`, and `basic.ass` carries the generator comment a real file is written with. Those bytes are what an ASS file from any fansub workflow actually contains, and the fixture exists to prove Sublore preserves an unknown section losslessly. Rename the section and the file no longer proves it about the one unknown section that exists in the world.
  - **Resolved rather than escalated, and here is the reasoning.** CLAUDE.md 3 makes losing a user's data the one unacceptable failure, and 5.3 requires fixtures to be real-shaped; both point the same way. The owner's rule is about not telling users whose design ours descends from, and a generator comment inside a test file says nothing about our design. So the two files stay as they are.
  - **The owner can overrule this in one line** and the cost is stated: the fixtures would keep testing "an unknown section survives" against an invented section name, which is a weaker claim than the one they make now.

- [ ] **N20 The range stop's precision is a design choice no check can defend (filed 2026-09-04).** mpv has no "play to here and pause", so `player.rs` holds a stop target and the event thread pauses when `time-pos` crosses it. That check sits on the unthrottled branch on purpose: mpv reports the position at frame rate, and the branch below it forwards to the UI only ten times a second, so a stop checked there would overshoot by up to a tenth instead of by a frame.
  - **Predicted and then measured.** Moving the check onto the throttled branch turned nothing red in `playhead.spec.js`, on seven passing tests. The prediction was written before the run.
  - **Why no check can see it.** The harness reads the transport slider, which is the throttled copy: a reading lags the player by up to the same tenth the mutation adds. The two designs are indistinguishable through that instrument. The test's own window says so, with the measured numbers beside it (11.70 read for a stop at 11.760).
  - **What would close it:** the app reporting its own stop, the way `preview.spec.js` reads mpv's account of the overlay out of the log rather than off the screen. A log line at the stop, with the position mpv had, would be readable and would make the frame precision assertable. Not built, because it is instrument work rather than product work.
  - Until then the choice is defended by a comment and by this entry. Anyone moving those lines to tidy the branch will not be told by a red test.

- [ ] **N19 One data home for twenty-four specs, and the app remembers what the last one left (filed 2026-09-04, half of it fixed 2026-09-04).** `wdio.conf.js:19` makes one `sublore-e2e-` directory for the whole run and every spec inherits it, so the app reopens whichever project the spec before it had open. That is a feature, tested by "reopens the project it had open", and it means a spec reading the emptiest state reads whatever its predecessors built.
  - **The guard that was supposed to absorb this could not tell two states apart, and that is fixed.** `closeAnyOpenProject` asked the DOM for `.rail__project` and returned when it was absent, which is both "no project" and "the project has not arrived yet": the restore is asynchronous, so a `before` hook inside that window closed nothing and the project landed two tests later. `command-registry.spec.js` failed on it. `project_session` now says at info level what it remembered, and the helper reads that line from this launch's own part of the shared log before it asks the rail anything. Three specs carrying their own copy of the helper use the shared one.
  - **What is still open is the data home itself.** One directory for the whole run is what makes every spec inherit its predecessors' state, and N24's parallel battery wants one per worker anyway.
  - **Found, not theorised.** `command-registry.spec.js` failed twice in the suite and passed twice alone, with `.rail__empty is missing from the DOM`. `chooser.spec.js` runs second and creates a project; the registry spec runs fourth.
  - **The remedy already existed and was kept private.** `project.spec.js` had `closeAnyOpenProject` with a comment saying in as many words that a previous spec may have left a project open. Lifted to `e2e/lib/rail.js` and used by the registry spec, which now makes the state it reads instead of inheriting it. That is the rule M3.7's own criteria state, and nothing outside `project.spec.js` was following it.
  - **Still open:** every other spec has the same exposure and only two of them noticed. A sweep would ask, of each spec, which state it assumes and whether it establishes it. `project.spec.js` keeps its own copy of the helper rather than the shared one, deliberately, because it is N15's spec and was not worth touching in the same change.
  - **Possible relation to N15**, unproven: N15 is `project.spec.js` failing wholesale in a suite and passing alone, which is the same shape. This is a mechanism that produces that shape. Whether it is the mechanism has not been shown.

- [ ] **N18 The registry has two routes it does not own, and a check that proves less than it says (filed 2026-09-04).** Both were declared by the agent that proved the registry with mutations, not found by review afterwards.
  - **CLOSED 2026-09-04 (commit 6364140):** the rail now resolves its items from the registry and its commands carry `project.*` ids, so the criterion holds for it. The check that refuses a greyed command grew the rail as a fourth route. What follows is the entry as it was filed.
  - **`RailMenu.tsx` never asks the registry.** It declares its own `RailMenuItem` type with its own `run`, and calls `item.run()` directly. So the criterion "every route ends at one gated call" is not merely unproved for the rail's context menu, it is untrue by construction, and no mutation could show it red because no registry command sits behind it. The rail's commands are the project ones, so this is where the project block will have to start.
  - **"refuses a greyed command from the menu, the toolbar and the keyboard" proves the keyboard only.** Deleting the availability check inside the dispatch turned that check red through Ctrl+Shift+S alone. The other two routes draw a real disabled button, so an X11 click on a greyed item dispatches no event at all and never reaches the dispatch. For those two the check proves the DOM greying is inert, which is worth knowing, and not that the gate holds. Only the keyboard has the gate as its sole defence.
  - Its own trace is the evidence: after clicking greyed Undo, the menu was still open, and a click that had reached the activate path would have closed it first.
  - **What would close it:** a route that can reach the dispatch with a greyed command. The rail's context menu will be one once it goes through the registry, because it is drawn as list items rather than as disabled buttons.

- [ ] **N17 Eight of the interface-size checks have never had their assertions run against broken code (filed 2026-09-03).** `interface-scale.spec.js` holds ten checks and both mutations that were tried turned the right ones red, so the spec is not weak. What is unproven is narrower and worth naming: eight of those ten call `pickSize`, which waits for the interface to actually be drawn at the size it just asked for. The mutation that flattens the root declaration breaks exactly that wait, so those eight die in the precondition and their bodies never execute. The clipping sweep at 90 and 150, the proportions check, the mid-drag hold on the video edge, the three-rows-of-grid check, the waveform floor equality and the tools-width ceiling are therefore assertions nobody has seen fail.
  - This is a property of the mutation, not a fault in the helper: a precondition that cannot be met should fail loudly and name itself, which is what it does. Driving each of the eight red wants a mutation that leaves the size working and breaks only the thing that check reads, which is six or seven more builds.
  - Reported by the agent that wrote the proof rather than found by review, which is the first time in this session that has happened.
  - One of the ten is weaker than its name suggests and the arithmetic says so: the floor check at 90 per cent has no power over a bound that stops scaling, because a flat 72 is already above the 64.8 the criterion asks for there. It only catches a bound that scales the wrong way. The same check at 110 and at 150 does the real work.
  - Also unmeasured: the mutation that stops `MIN_CURRENT_LINE` scaling was only ever run against this one spec. `dividers.spec.js` and `waveform-sash.spec.js` read the same bound through `e2e/lib/scale.js` and may have gone red too, which would be coverage rather than a problem.

- [ ] **N16 The scaled surface check leaves a process behind, about one run in three (filed 2026-09-03).** `pnpm e2e:scale` runs the app twice, at `GDK_SCALE=1` and at 2, and its fifth assertion is that both runs exit 0 and leave nothing in their process group. That assertion fails intermittently with exactly one survivor, in one of the two runs, not consistently the same one. The four assertions the check exists for, that the surface doubles in size and in position with the ratio, pass every time.
  - **Not caused by the preview work, measured rather than assumed.** On the branch tip: one red in three. On a clean worktree at `d7e252e`, which has none of the preview code, two red in three, with the same shape (`survivors [[],[1629680]]`). The failure predates the change that first surfaced it.
  - The survivor looks like a WebKit auxiliary process: the log shows it as `(process:NNNN)` complaining it cannot reach the accessibility bus, a child of the app rather than the app itself. It is not mpv, which the check never starts a second copy of.
  - Counter-evidence worth keeping: `pnpm e2e:shutdown`, whose entire job is exit cleanliness and which asserts the same thing, passed every run of the same battery. So either the two checks tear down differently, or the window is narrow enough that only the scale check's back-to-back double launch hits it.
  - **What it needs:** repetition under contention, the same shape N13 needs, plus reading how each check decides a process group is empty. Not reproducible on demand, so a single green run proves nothing about a fix.
  - Found on the way, and worth knowing before anyone repeats this: about four `xvfb-run` launches in quick succession on this machine start failing with `Failed to initialize GTK` and the app never opens a window. That is the harness running out of room, not a defect, and it makes long repetition loops read as failures.

- [ ] **N15 `project.spec.js` failed wholesale once in the suite and has not since (filed 2026-09-03).** All eleven of its tests red in one full-suite run on the M2.4 W8 branch, and green everywhere else: eleven of eleven when the spec runs alone, and four of five full-suite runs on the same branch afterwards. `main` is two of two, which is too few runs to call a difference.
  - **Seen a second time, 2026-09-03 evening.** All eleven red in a full-suite run on `ui-rebuild` with the interface-size work in the tree, and eleven of eleven green when the same spec ran alone against the same binary minutes later. The interface size was the obvious suspect, because the rail is driven by coordinates and everything had just grown by a tenth, and it is ruled out by that isolated run. Two sightings now, both wholesale, both green in isolation: whatever this is, it is about the spec's neighbours in a suite run and not about the spec.
  - The suspect that reading refuted: W8 gives `audio_tracks` its first caller in the frontend — it was a command nobody invoked — and `Player::handle` clones the mpv handle out of the mutex and reads properties with no lock held, alongside the event thread. But `libmpv2` declares `Mpv` both `Send` and `Sync`, and the handle was already shared with that thread and with every other command, so this adds a caller to a shared path rather than sharing something new.
  - Not chased further on the machine that saw it: one occurrence in five is a rate nothing can be concluded from in an afternoon, and the same shape has twice turned out to need contention this box does not have (N11, N14).
  - AC: either it reappears in CI, where the runs are many and the machine is contended, and is then diagnosed from the app log the job now collects; or it does not, and this entry is closed by a count of green runs rather than by silence.

- [ ] **N13 Playback is sometimes already stopped between two tests that nothing stopped it in (filed 2026-09-03).** `e2e/specs/video-surface.spec.js`'s pause test needs the video playing, which the test before it leaves behind: that test's last assertion is that mpv's clock is still moving. Between the two, only `afterEach`'s stage restore runs, and it touches no transport. Yet in CI the pause test sometimes finds the transport reading "Play", meaning stopped, and its click then started playback instead of stopping it. That is what kept `main` red intermittently from 2026-09-02: the failure said `timed out waiting for the transport button to read "Play"` and named neither the value it saw nor the cause, so it was twice mistaken for a flake and once dismissed after a green re-run.
  - Diagnosed 2026-09-03 by making the test say what it read: `Expected: "...: Pause"` against `Received: "...: Play"`. So the click was landing; the state was already wrong when the test began.
  - The test now establishes its own precondition rather than inheriting it, and prints one line with the position and the duration when it has to. A green run that prints that line has still seen the anomaly, which is the point: the flake is gone from the verdict and the question is not.
  - Not the cause, and these are ruled out with a measurement each: the fixture running out (60.023 s long, the test runs about fifteen seconds in), the peak job saturating the runner (the smoke step grows at T5, which was green, and not at W4, where the red began), `audio_tracks` disturbing mpv (it reads properties and sets none), and the spec being broken on its own (six consecutive runs in isolation on `main`, all green).
  - **What the line has reported, 2026-09-03.** Four occurrences across eight green CI runs, and the positions are all the same: 1.03, 1.07, 1.13 and 0.7 seconds of 60.023. So playback does not fade out somewhere along the file — it starts and dies about a second in. The fixture is `sample.mkv`, which is 60.023 s long, so this is nowhere near its end.
  - Refuted by reading the code: nothing in the app pauses on the way through. The test before this one never pauses — it clicks Play, waits for the clock, hides and shows the surface, and waits for the clock again — and `set_property("pause", true)` appears in exactly two places, opening a file and the pause command itself.
  - Does not reproduce here: six runs of the spec on this machine, no occurrence. Same shape as N11 and N14, both of which needed contention before they would show, and both of which were then named in one run of it. That reproduction is the next step and it belongs to a night.
  - Instrumented meanwhile: mpv pausing itself now says so, with the position, in the app's own log — which CI has collected since #64. Until this, an unasked-for pause left no trace anywhere, which is why four CI runs could see it and say nothing about it.
  - AC unchanged: the cause is named before the reporting is removed. Deleting the line because the suite is green would be closing the entry by looking away.

- [ ] **N11 One SIGSEGV on exit in `mpv-context`, on a branch that touches no teardown code (filed 2026-09-02).** Running the raw check battery for T5 on a loaded machine, `pnpm e2e:mpv-context` failed with `exit {"code":null,"signal":"SIGSEGV"}` after its first three assertions passed, so the crash is on the way out rather than a behavioural failure. Six sequential reruns immediately afterwards were 6 green, 0 red, and the branch it was seen on changes only `src/` and `e2e/`, no Rust and nothing in the teardown path, so it is not that branch's defect.
  - It is filed rather than attached to N1b, which is closed. N1b was the close gate's **save** branch, measured at 2 in 30 under six concurrent streams and explicitly not reproducible sequentially; this was a different check, on the plain exit path, and one occurrence is not a rate. What the two share is the shape: a crash after the work is done, on the way out, with load the visible difference. N1b's own status line already carries a correction saying the delivered binary was run three times sequentially rather than the thirty its criterion asked for, so the evidence that closed it was thinner than the checkbox suggests.
  - The load at the time was real and is worth naming: the machine's one-minute average was above 20 while the battery ran, on 16 cores.
  - **Second sighting, 2026-09-03, in CI.** `pnpm e2e:scale` on the run that removed the D-Bus stall: all four of its assertions passed and the process then died on the way out, `exits [{"code":null,"signal":"SIGSEGV"},{"code":0,"signal":null}]`. Two occurrences is still not a rate, but it is no longer one, and it happened on the first run where startup was 29 seconds shorter, so teardown now follows startup much sooner. That is a variable worth naming when this is reproduced.
  - **Third sighting 2026-09-03 on #64, both launches at once**: `exits [{"code":null,"signal":"SIGSEGV"},{"code":null,"signal":"SIGSEGV"}]`.
  - **Reproduced, named and closed, 2026-09-03.** It does not reproduce on an idle machine; under concurrent load it does, and `pnpm e2e:n11-battery` is that load in the repository rather than a shell loop nobody else can run. Fifty runs in five streams: **6 closed on a signal before the change, 0 after**, and the six runs that had to be killed at teardown went to zero with them.
  - The frame is `_gdk_x11_display_queue_events`, identical in four core dumps at `libgdk-3.so.0.2420.32 +0x7271a`. The faulting instruction is `cmp (%rax),%rsi` right after `mov (%rcx),%rax` and a null check, which is `G_TYPE_CHECK_INSTANCE_TYPE` inlined: the object's class pointer is not null and is not mapped either. A GObject freed under GDK while it is still walking its window list.
  - Which object was found by taking it away. `Surface::destroy` called `gdk_window_destroy` and then dropped the last reference to the wrapper. Not destroying the window at all: 0 core dumps in 50 launches, but 3 runs left a process behind, so that trade is a crash for an orphan. Destroying the window and keeping the wrapper alive: 0 core dumps and no orphans, which is the change made.
  - Ruled out with a measurement each: the waveform fallback landed in #64 (17 dumps in 50 launches before it, 21 after, 0.8 standard errors apart), and the teardown order, which the code already gets right — mpv is shut down first and the surface only goes if it did.
  - Left as an observation rather than a claim: under a much heavier load than the battery's — the check pinned to two cores with a busy loop contending them — 2 or 3 runs in 25 leave a process behind, and they do so before the change and after it (0, 2, 3 and 2 across four arms of 25). The battery's own six hangs go to zero with the crashes, so the two instruments do not agree, and starving the app to a third of two cores is not a condition CI is in. Whether that is one fault or two needs its own measurement before it gets its own entry. If it cannot be reproduced in a battery sized to catch a defect at the rate N1b was measured at, that number is reported and this is closed as unreproduced rather than as fixed.

- [x] **N10 Thirty seconds of D-Bus before every CI launch, and none on the owner's machine (filed 2026-09-02, measured 2026-09-03).** Between GTK initialising and `Sublore 0.1.0 starting on linux` reaching the log, the eleven pid-matched launches of CI run 33639531468 spent 13.08, 27.06, 28.67, 29.34, 29.45, 29.49, 29.57, 29.81, 29.86, 29.96 and 29.97 seconds, so this entry's original "between 28.7 and 30.0 across eleven launches" was wrong about its own run and is corrected here rather than quietly dropped. Across 676 launches in 31 jobs between 2026-08-30 and 2026-09-02 the same interval never exceeds 30.17 s, with a median of 30.06 and a mode of 30.06 plus or minus 0.01, and two launches seconds apart in one step on one runner measure 6.33 s and 30.06 s. That is a fixed timeout, not a loaded machine. It costs roughly 630 of that job's 1030 seconds and 73 per cent of the 1226 s job on run 33690642329, and it scales one for one with a check list that has nearly tripled since the first CI run.
  - Measured on the owner's machine 2026-09-03, verified on Linux, with the existing debug build: exec to the first log line is 0.238 to 0.243 s, and the CI-shaped interval from GTK's dbind warning to that line is 0.068 s under CI's own `xvfb-run -a -s "-screen 0 1920x1080x24"` and 0.243 s on the real display, 0.233 s with the NVIDIA workarounds armed. The release binary puts its window on screen at 0.205 s. The first AC is met: the pre-log phase is two orders of magnitude inside CONTRIBUTING.md §7's 2 s, this is a CI cost and not a product defect, and the entry's stated cause, Xvfb plus software rendering, is struck because the local Xvfb run reproduces the identical DRI3 software fallback and costs 68 ms.
  - What waits, from a dbus-monitor capture of the pre-log phase on this machine: `GetNameOwner org.gnome.SessionManager`, `GetNameOwner org.xfce.SessionManager`, then `AddMatch` and `StartServiceByName` for `org.freedesktop.portal.Desktop` on the `org.freedesktop.portal.Inhibit` interface. That is GtkApplication's session tracking; all three names are in libgtk-3 and none is in libwebkit2gtk-4.1, so the waiter is `app.register(cancellable)` at tao-0.35.3 `event_loop.rs:229`, reached from tauri `app.rs:2338-2342` inside `build()`, before the window at `app.rs:2523` and before the log line at `src-tauri/src/lib.rs:167`. The logs agree: in CI and in the local reproduction the libEGL lines land after the wait, so the stall is in front of EGL and the webview, not inside them.
  - The mechanism is proved on this machine, both directions. With a session bus where `org.freedesktop.portal.Desktop` is declared activatable by a `.service` file whose Exec never claims the name, the same binary takes 30.239 s to its first log line, 30.073 s of it between the dbind warning and libEGL, which is CI's mode to within 13 ms, printing CI's lines in CI's order including the `org.a11y.Bus` ServiceUnknown warning at 0.135 s. Delete that one file on the same bus with the same config: 0.223 s. Against a bus with no service directory at all: 0.229 s.
  - It is not proved to be CI's instance and is written down as a lead. `xdg-desktop-portal` 1.18.4 and `xdg-desktop-portal-gtk` 1.15.1 arrive on the runner as dependencies of `libwebkit2gtk-4.1-dev`, visible in the e2e job's own apt output, so the name is activatable there while nothing can claim it in a headless job with no session, and every stalled CI launch gets an error reply from a live bus at the moment its stall begins. Nobody has moved that file aside in a CI run, which is the only thing that would settle it.
  - Killed with a measurement each: the NVIDIA hatch (0.233 s armed against 0.243 s disarmed, and `e2e/lib/env.js:31` pins `SUBLORE_WEBKIT_WORKAROUNDS=0` on both machines, so CI and the harness always take the same branch and only the printed word differs); the accessibility bus in its ServiceUnknown form (CI's line verbatim against an answering bus with no service files, 0.229 s); libEGL DRI3 (printed in every fast run too); process start and dynamic linking (the no-display refusal at `src-tauri/src/main.rs:90` is a 0.59 s step). Also killed as CI's cause: `dbus-launch`'s autolaunch retry loop, six forks each ending in a 3.000 s `clock_nanosleep`, which is real here (18.292 to 18.315 s) and was removed and restored on one permission bit, but which only runs when no bus answers, and CI's bus answers.
  - Not explained by anything above: the 34 of 151 launches that escape at 5.3 to 28.2 s and recur at the same positions in the job, the 13.08 s launch included.
  - Machine note, not the app's defect and left alone: `/home/alcahest/.dbus` is root owned and mode 700 on the owner's machine, so any application that has to autolaunch its own session bus there pays 18.3 s at startup.
  - **Proved in CI, 2026-09-03, and closed.** The mechanism was reproduced on the runner itself and not only here: one job ran the shutdown check twice, once with `/usr/share/dbus-1/services/org.freedesktop.portal.Desktop.service` moved aside and once with it in place, everything else identical. `shutdown-no-portal: exit 0 after 0m02s` against `shutdown: exit 0 after 0m31s`. That is the 29 seconds, on CI's own hardware, so the attribution that this entry called a lead is now a measurement. The file is moved aside for the whole job, before the fixtures, and the app is unchanged: it reaches its first log line in 0.24 s on real hardware and dodging a runner's condition from inside `src-tauri` would make the harness drive a configuration no user has.
  - AC (met 2026-09-03): the interval is measured on the owner's machine and reported with its number. It is under 2 s, so by this entry's own terms the budget question closes and what is left is CI cost.
  - AC: the cause in CI is named by a CI run that removes it and a CI run that has it, not by inference from a local reproduction. The cheapest form is one step that prints `ls /usr/share/dbus-1/services` and one check run with `/usr/share/dbus-1/services/org.freedesktop.portal.Desktop.service` moved aside, compared against the same interval in the other steps of the same job.
  - AC: nothing is added to `src-tauri` or to `e2e/lib/env.js` to work around a runner condition. The app is 0.24 s for a user, and a harness that arms a hatch to make CI faster would be testing a configuration no user has.
  - AC: whatever the answer, the check that proves it is a measurement with its own number in the log, not a pass or fail against a figure somebody chose.

- [ ] **N9 Assertions anchored to something the environment decides (filed 2026-09-02).** M3.7 is one member of a class, and the class was read out in full before it produced any more CI runs. The five failures of 2026-09-01 and 2026-09-02 — the picker's pixel coordinate, the 600 ms stall inherited by fourteen ffmpeg tests, a directory named with a newline and a quote, the `asr.spec.js` chain, and a `fixtures/video` listing pinned to a literal array — are one defect: an assertion whose truth depends on a quantity the machine chooses rather than on the behaviour under test. The audit is in the planning archive as `docs/environment-anchored-assertions.md`, with every claim carrying a path and a line, and it is ranked by cost: a test that goes red on a slow runner costs an hour, and a test that goes green while the code is broken costs a release. Seventeen of the first kind and eleven of the second, plus what to leave alone and why, because several of these can only be checked by driving a real display and saying so is part of the answer.
  - The five that matter most, all of them silent. `crates/sublore-asr/tests/no_network.rs:85-98` constructs a `NeverFetcher` and never passes it to `ordinary_workflow`, so the assertion that nothing reached the transport cannot fail under any change to the product; the criterion is genuinely covered by the socket-counting test beneath it, so what is lost is the second proof the header promises. `crates/sublore-io/tests/atomic_save.rs:549-557` and `:608-616` return green with zero assertions whenever the process can write into a `0o500` directory, which is what root gets, and the `eprintln!` that says so is swallowed by libtest — two of CONTRIBUTING.md section 3's own criteria, silently unverified, and a skip section 5.4 forbids. `crates/sublore-asr/src/sidecar.rs:759-763` asserts `(1..=8)` around a function that clamps to `(1, MAX_THREADS)` with `MAX_THREADS = 8`. `e2e/scripts/n1b-load-battery.js:77-87` answers "no core dump" when `coredumpctl` is not installed, which is half of N1b's closing criterion satisfied by a missing package. `e2e/scripts/n1b-load-probe.js:37` still clicks `{750, 540}` where `e2e/lib/paths.js:37` records `{840, 523}` for the same window after T1 moved it, and `:80` still waits the 3500 ms that `close-gate-check.js:162` records as having failed the first real CI run.
  - Two findings change what is at risk rather than what is broken. `.github/workflows/ci.yml:155-156` runs `cargo test --workspace` on `windows-latest`, so the Rust suite runs there and every Windows filesystem rule in it is live today, not MW.1b's problem — which is what the newline-and-quote failure was. And there is no frontend test runner at all: no vitest, no jest, no `*.test.*` under `src/`, so every line of TypeScript is covered by the E2E suite or by nothing, which is why the E2E suite's silent failures cost more here than they would elsewhere.
  - The undiscovered sibling of the fifth CI failure is `src-tauri/tests/subtitle_commands.rs:270-282`, which lists `fixtures/subtitles/srt/clean` and fails on any entry that is not a `.srt`. Adding a README there turns it red for a reason unrelated to a save writing beside a source. The repository already holds the right shape in four places — a floor plus a relationship, never an inventory (`subtitle_commands.rs:85-89`, `formats/tests/common/mod.rs:56-61`, `edit/tests/mutation.rs:78-80`, `formats/tests/ass_roundtrip.rs:114-132`).
  - AC: the seven changes the audit's own closing section names are made, each as itself, and each is shown to fail before it is shown to pass. A test that could not fail before the change must be made to fail by breaking what it claims to cover, and the run that proves it is recorded.
  - AC: no assertion is weakened and no test is skipped to close this. Where an environment genuinely cannot express a case, the run refuses by name with an actionable sentence, the way `e2e/lib/paths.js:44-49` and `wayland-attach-check.js:53-71` already do, rather than returning green.
  - AC: the two remaining hardcoded pixel constants (`e2e/lib/paths.js:37`, `e2e/scripts/picker-thread-check.js:70-72`) each carry a comment saying how the number was measured and against which window state, not only that it will need re-measuring. `paths.js:37` is already the model; `picker-thread-check.js` is not.
  - AC: M3.7 is answered as part of this rather than beside it. The `asr.spec.js` chain is fifteen tests long, not nine, and the decision to record is how many of them are worth making independent — several are sequential because the behaviour under test is a sequence.
  - AC: the audit's three open questions are answered from a real run and written down: what `setlocale(LC_NUMERIC, "")` resolves to on each CI image, whether the two `create_safety.rs` races ever observe contention on a hosted runner, and what `PLANTING_ATTEMPTS = 20_000` costs on `windows-latest` inside a 30-minute job.

- [ ] **N5 Residue of gate 2, filed rather than fixed (2026-08-30).** Every row here was found during gate 2, and each is a minor by the owner's ruling that the gate opens on blockers and serious, not on minor perfection. Grouped because they share owners and would otherwise reopen the same files one at a time.
  - ~~The three `player.rs` rows: the two env hatches disagreeing, the non-UTF-8 value printing as unset, and an unrecognised `gpu-context` costing playback with no fallback.~~ **Fixed by `b343b0c` (2026-08-31), struck 2026-09-01.** All three are closed in the tree, and `mpv-context-check.js` asserts the fallback green in CI. Struck rather than deleted: a row that vanishes gets refiled by the next reader who finds the old report.
  - `useStartupFiles.ts:63` — a file named on the command line that fails to open reports only to `console.error`, which no user of a release webview can read. Surfacing it needs the hook to hand the failure to `App.tsx` and the copy to `en.ts`, so it is three files and a rendering decision, not a line.
  - `e2e/lib/pixels.js` — `saturation()` now has no caller anywhere; `requireFfmpeg()` still has one. Dead code by CONTRIBUTING.md section 6. `e2e/README.md:7-10` still explains ffmpeg as the harness's pixel instrument, which is no longer why it is needed.
  - `VideoStage.tsx` — the `matchMedia` listeners added for a live device-ratio change are fired by nothing. Exercising them needs a frontend test runner with a fake `matchMedia`, which is a new dependency and an owner decision, or an XSETTINGS rig that flips the scaling factor under a running app.
  - `e2e/wdio.conf.js:33` — the ffmpeg gate stays. With `saturation()` dead it reads like a guard on a dependency nothing needs, and it was read that way once during gate 2; a run with ffmpeg hidden contradicts that, because `asr.spec.js` runs a real extraction through it. Delete the gate and a named error becomes a timeout inside whichever spec hit it first.
- [x] **N6 `ExitRequested` never prevents the exit (latent data-loss path, filed 2026-08-30).** The close gate protects `CloseRequested` and nothing else. `RunEvent::ExitRequested` has no `prevent_exit`, which is safe today only because nothing calls `AppHandle::exit`. The moment a menu item, a tray entry or a keyboard shortcut quits the app — and M2.0 builds a menu bar — the gate is bypassed and unsaved work leaves with the process. Found during gate 2 while reading something else.
  - AC: quitting through every route the app offers asks the same question the window's X button asks, proved by a check that drives the non-X route.
  - AC: the check fails if the gate is removed from that route.
  - Status 2026-09-02, verified on Linux: the quit is not given a gate of its own — `ExitRequested` with an exit code closes the app's windows and prevents the exit, so the quit route arrives at the same `CloseRequested` arm the X button does, with one dialog, one answer path and one save. `e2e/scripts/quit-gate-check.js` drove `AppHandle::exit` through a debug-only hook (`SUBLORE_QUIT_ON_FILE`) until M2.0's T3 built the caller it was written for; it now drives the File menu's own Quit item from the keyboard, and Ctrl+Q in the save branch, and asserts 17 checks: the quit asks, cancel keeps the app and the file, a second quit asks again, discard exits 0 leaving the file alone, save writes the edit and moves nothing else, and a quit with nothing unsaved still exits 0 instead of being held by its own routing. Second AC proved by removing the arm and rebuilding: the check went red naming the lost edit, and `cargo test` went red with it. Windows compiles (clippy, `x86_64-pc-windows-gnu`); no behaviour run there.

- [x] **N3 Non-cue segments: written analysis (decision 10).** Half a day, output is a document, not a feature. Answer whether `Edit` and `Expectation` in `sublore-edit` can grow a `Meta` variant covering `Style:` lines, script properties and attachments, which today travel as uninterpreted metadata (`sublore-formats/src/document.rs:81-92`) while `Edit` covers cues only (`plan.rs:28-58`). Adjust the shape now if the answer says so; the feature itself stays at M14. Not blocked by N1 or N2 and can run alongside, but **must land before M5**, because once the closed modules depend on the crate a second write path gets bolted alongside the first instead of replacing it.
  - AC: a document in the planning archive states whether the shape changes, and if it does not, why not. "No change needed" is a valid and useful answer, in writing.
  - Status 2026-09-01: written, in the planning archive as `docs/non-cue-segments.md`. The answer is that the shape does not change now: the mutation pipeline is five lines over a byte splice and every guard under it is already indifferent to whether the bytes belong to a cue, and `Expectation` with `removed: 0` and an empty cue list already states the strongest constraint a `Style:` rewrite can be held to. What must change before M5 is the write surface of `sublore-module-api`, which N8 designs.

- [x] **N8a The interface crate (decision 21).** `crates/sublore-module-api`: the `#[repr(C)]` tables and values, the ABI version, the two exported symbol names and the closed list of refusal codes that `module-abi.md` fixes. Types and constants only, and nothing in the workspace depends on it yet — the fixture module is N8b and the loader is N8c.
  - AC: adding, reordering or widening a field in any `#[repr(C)]` type fails `cargo test -p sublore-module-api` and names the type, and so does a table whose declared size stops matching its own `size_of`.
  - AC: a string with a NUL in its middle crosses the boundary whole, because a cue can hold one: `SourceText::from_bytes` scans only the first 1024 bytes of a file for NULs, so a C string on the boundary would truncate that cue.
  - Status 2026-09-02, verified-by-tests **on Linux**, compiled for `x86_64-pc-windows-gnu`: ten unit tests, and both pins were shown to fail by widening two structs before being restored. This crate has no behaviour in the app to verify and no in-app criteria of its own; the boundary's behavioural criteria are the loader's and land with N8c.
  - **Closed 2026-09-04.** The three fields §0 of `module-abi.md` owed are in: `SubloreLine::flags` with `SUBLORE_LINE_FLAG_FILE_MISSING`, `SubloreInvocation::row`, `SubloreHost::ctx`. The three pinned sizes moved to 64, 40 and 128, which are the numbers the document had computed, so the design and the compiler agree.
  - **The layout pin did not hold what N8a said it held, and a mutation is what said so.** Its criterion reads "adding, reordering or widening a field in any `#[repr(C)]` type fails `cargo test` and names the type". Widening `SubloreLine::flags` from `u32` to `u64` failed nothing: the four extra bytes land in padding that was already there, so the size stays 64 and the alignment stays 8. An offset pin was added and **still let it through**, because every one of the six offsets is also unchanged. Two fields of the same width swapped is a second case the size pin cannot see and the offset pin does catch.
  - **What holds it now:** a never-called function that destructures every table and binds each field to its declared type. A field added, removed or renamed stops the destructuring compiling; one whose type changes stops the binding compiling. That is the only one of the three that catches a field widened into its own padding, which is a real ABI difference a closed module would be miscompiled against in silence.
  - Two places where `module-abi.md` contradicts itself, resolved in the code and named here rather than fixed silently. §2.2 types a database row as `SubloreCell`, the panel cell of §5.3, while §4.7 defines `SubloreValue` with SQLite's five storage classes; the crate pushes rows as `SubloreValue`, since a panel cell has no null and no real. §4.4's `SubloreLine` has no field for the missing-file flag its own prose requires, and the struct is built as written, so N8c has nothing to report a missing file with.

## M2 — Editor with video and waveform (tasks detailed 2026-08-28)

M2.0's precondition is met. `x11-vs-render-api.md` recorded four stale citations that M2.0's preparation documents carried — `video/mod.rs:106` in `decisions.md`, `shell-layout.md` and `post-v1-plan.md`, and `video/mod.rs:196-197` in `shell-layout.md`. All four were corrected in the planning archive on 2026-09-01, against the code as it stands: `d224f3c` derives the surface's visibility, so the two calls those addresses named have one caller each now. T1 has landed.


Goal: the free product's core: cue list, text editing, timing adjust against waveform, side-by-side source/target view.

- [ ] **M2.0 Shell redesign — blocks M2.4-M2.6.**
  - Filed 2026-08-29 after the owner ran the app and rejected the interface. M0-M4 each bolted a horizontal band with a path field and a button onto one column, because that is the cheapest way to give an E2E spec a stable selector; the result is the union of five test harnesses. Rebuild the shell around a panel layout with the project rail on the left, the video beside it taking the full height of the top block, waveform and current-line band stacked to its right, and the cue grid across the bottom, with a modern finish, per `shell-layout.md` and `shell-mockup.html`. The engine is not touched: this is the frontend shell plus the file-dialog commands.
  - **Preparation status (updated 2026-09-01): the breakdown exists, in `m2-0-tasks.md`, as tasks T1 to T9 each with its own criteria.** The constraint CONTRIBUTING.md §5.1 imposes is unchanged — nothing is built before its criteria are written — and only the address has changed: it used to say the breakdown did not exist, and it does. T1 landed on 2026-09-01. The design it implements is `shell-layout.md` and `shell-mockup.html`; the criteria below are the milestone's, the breakdown's are each task's.
  - **T2 landed 2026-09-01, verified on Linux.** The one-column stack is the five regions the layout names — chrome, rail, video, tools column, grid — with the status bar of decision 24 A1 across the bottom outside them, carrying the document line and the unsaved marker on the left and the transient messages and errors on the right. `App.css` is split per region under `src/styles/`, over a dark-only colour token layer (decision 22). The tools column is empty until the waveform (M2.4) and the current line (T5) arrive in it; the chrome region holds the old command bars until T3 and T4 replace them.
  - **T4 landed 2026-09-02, verified on Linux.** The transcription band is off the screen: `Transcribe…` in the Edit menu opens a panel that is absent until it is asked for, and that takes space under the grid when it is, so opening it never moves the video surface. It is not a layer, deliberately: an HTML layer over the video is neither visible nor clickable until decision 1's occlusion lands (T8), measured here — a box on the stage rectangle took no click at all, while the same box over the grid took every one. The menu item is in Edit because File has no room for it: `chrome.spec.js` walks Open video down to Quit over the disabled pair, and `quit-gate-check.js` reaches Quit as the last enabled item in File, so an item anywhere in File breaks one of the two. It waits there for the Audio title (decision 24 A2). Decision 24 A7's second half is **not** built: progress and cancel do not appear in the status bar while the panel is dismissed, so a run in flight is watched and stopped by reopening the panel. `asr.spec.js` keeps its fifteen tests with their assertions and gains two, one per T4 criterion; `EXPECTED_TESTS` 80 to 82. `firstCueText` in `e2e/lib/paths.js` moved up 87 px, measured off two screenshots of the launch the close-gate scripts make.
  - **T5 landed 2026-09-02, verified on Linux.** The tools column holds the current line, and the waveform is absent from it rather than empty. The box edits whichever row carries the grid's cursor, and committing there issues `subtitle_set_text` — the one command the grid's own editor commits with, asserted as the only subtitle command that crosses the boundary while the commit happens, and taken back by a single undo. Decision 5's two states moved out of `CueList` into the shell, where `shell-layout.md` puts them, so the grid row and the box are two views of one row and not two states; opening a document resets them the way the grid's remount used to. Both editors now carry `data-document-editor`, so Ctrl+Z and Ctrl+S inside either belong to the document rather than to the webview, and undo, redo and every save flush both. **The times are read-only, so half of the first criterion is not met.** `subtitle_set_times` is registered and no frontend file calls it: that hole is M2.7's, and opening it here would have built M2.7's route without M2.7's criteria, so start, end, duration and CPS are drawn and cannot be typed into. The absence of the waveform is asserted as "nothing else takes space in `.shell__tools`", never as a missing selector, and the check shows itself catching a panel it inserts before it reads the real column. `EXPECTED_TESTS` 82 to 86: three checks in a new `current-line.spec.js` and one in `shell.spec.js`. Lifting the cursor into the shell means a cursor move re-renders the shell rather than only the grid; the M2.3 open, scroll, typing and round-trip budgets in `editor.spec.js` were unaffected, and a cursor move has no budget of its own.
  - **T8 landed 2026-09-02, verified on Linux.** The picture gets out of the way for an HTML layer: with the fixture playing, opening the File menu over the video rectangle leaves the dropdown on screen and the native surface `IsUnMapped`, and closing it brings the frame back with mpv still attached and the clock still advancing. The shell holds the set of open layers (`src/hooks/useLayers.ts`) and each one registers itself with `useLayer` while it is open, so mounting is the whole contract and a layer added later cannot forget; the four today are the menu bar's dropdown, the rail's menu, the rail's question and the About dialog. Visibility stays derived: `video_set_layers` is a **third input** to the one `SurfaceState` that `settle` reads, never a show or a hide, so the empty stage is still never covered and an open under a layer cannot leave the surface visible over it. No overlap arithmetic — every layer hides the surface whether or not it covers the video, which is the accommodation decision 1 chose and `shell-layout.md` gives the reason for. While a layer is open the stage rectangle keeps being measured and stops being sent, and it goes back with the uncover, so the frame lands on the layout as it is then rather than as it was. **The transcription panel is deliberately not a layer**: T4 built it to take space under the grid rather than cover anything, `shell-layout.md` excludes what is resized away from the word, and registering it would blank the video for the length of a run the user is watching. `video-occlusion.spec.js` carries three checks, `EXPECTED_TESTS` 86 to 89: the criterion itself, with the dropdown's overlap of the stage read off both elements rather than assumed; the swap from the menu to the dialog it opens, where twelve samples at 100 ms all read `IsUnMapped` — a poll cannot prove a negative over an interval, so the claim is worded as what was sampled; and the frame coming back on the rectangle measured while the menu was down. Map state and mpv's own child window, never pixels, for the reason `video-surface.spec.js` states. Second criterion proved: dropping `!layer_open` from `wants_shown` and rebuilding turned all three red, each naming the surface that never hid, and the unit test over the three reasons went red with them. Windows compiles (clippy, `x86_64-pc-windows-gnu`); no behaviour run there. **Not exercised:** an open that starts while a layer is up would load mpv into a hidden surface — no route in the interface reaches it, because every layer closes before its command runs and the chooser is a native window of its own.
  - AC: opening a video or a subtitle goes through the system file dialog, reachable from the File menu and from the toolbar; no field for typing a path is left anywhere in the interface.
  - AC: the project rail is on the left; the video sits beside it and spans the full height of the top block; the waveform and the current-line band are stacked to the video's right; the cue grid takes everything below. The current-line band is **not** full width under video and waveform together.
    - **Correction 2026-09-01.** This criterion used to read "video and waveform panels sit side by side in the top band, the current-line band under them", which is the arrangement `shell-layout.md` explicitly rejects: "The edit box is not full width under both — that was the error in the first draft of this document." The task description a line above always described the right shape, so the criterion the work would have been judged against contradicted the plan it belongs to. Written out rather than quietly replaced, because a criterion nobody re-derives is a criterion somebody reinstates.
  - AC: transcription controls are not on screen until opened from the menu.
  - AC: with a video and a subtitle open, at 1024x700 and at 1920x1080, no element is clipped at a window edge and the page never scrolls horizontally (the clipped `Save copy to` label at 1024x700 is the regression fixture for this).
  - AC: the panel holding the video never scrolls, per the M0.2 constraint that the native X11 surface is placed from DOM coordinates and recomputed only on resize.
  - AC: the existing E2E checks pass with their assertions unchanged — the count is whatever `EXPECTED_TESTS` in `e2e/wdio.conf.js` says on the day, 33 at 2026-08-30, and it never decreases; no assertion is weakened, skipped, or retargeted (CONTRIBUTING.md §5.4). Selectors move, and for the checks that open a file **the route moves too**: with the path fields gone there are two ways in, the command line through `startup_files` and driving the system chooser as `e2e/scripts/picker-thread-check.js` does, and each affected check picks one.
    - **Correction 2026-09-01.** This criterion used to say the checks pass "with selectors re-pointed and assertions unchanged", which cannot hold beside the criterion above it. Five checks reach the app by typing a path into a field the redesign deletes: `subtitle.spec.js` writes into `.subbar__input` and `.subbar__dest`, `project.spec.js` into `.project__path` and `.project__file-path`, and `video.spec.js` into `.bar__input`. Re-pointing a selector cannot find a field that no longer exists, so the work would have hit this on its first day with no plan for it. The two routes named above are the ones that already exist and are already exercised.
  - AC (decision 1, occlusion): opening a menu or a dialog over a playing video hides the native surface, and closing it brings the frame back. E2E: with a video playing, open the File menu over the video rectangle — the menu is visible and the video is not covering it; close it and the frame returns. No native system menus and no separate popup windows: the CSS chrome is what keeps Windows and Linux looking the same. Depends on N2.
  - AC (decision 5, selection): the shell separates the active line (single cursor) from the selection (a set: single, shift for a range, ctrl for scattered), both drivable from the keyboard. Bulk operations act on the selection. Landing it here, before M2.5 leans on it, is the point.
- [x] **M2.7 The cue structure edits reach the interface (filed 2026-09-01, owner ruling).** `lib.rs` registers `subtitle_insert`, `subtitle_delete`, `subtitle_split`, `subtitle_merge` and `subtitle_set_times`, and no frontend file invokes any of the five. M2.1 built the mutation API and M2.3 gave text editing a route; these five have no gesture, no menu item and no button, so a user cannot split a cue, merge two, insert one, delete one, or change a timing without editing the file by hand. Filed as its own task rather than folded into M2.5, because the backend is done and what is missing is only the route.
  - AC: from the interface alone, with a subtitle open, a user can insert a cue, delete one, split one at the cursor, merge two adjacent ones, and change a cue's start and end. Each lands in the document and is visible in the grid.
  - AC: each of the five is undoable with the same undo the text edits use, and redoable, and the file on disk is unchanged until a save.
  - AC: E2E drives all five through the interface and asserts the saved file afterwards; a test that calls the command directly does not satisfy this criterion, because the hole being closed is the route, not the command.
  - AC: inserting or deleting a cue moves the cursor and the selection with the rows, so neither points at a different line than it did before, and neither is left past the end. T6 left the two states indexed by row and nothing in the interface changes the row count yet, so the remap is owed here rather than built ahead of a caller.

- [x] **M2.1 Editable document model.** Mutation API over `sublore-formats` (edit cue text, edit times, insert, delete, split, merge) that keeps the lossless guarantee: everything the parser preserved stays preserved for untouched cues, and every mutation re-runs the tiling/coverage guard M1 added.
  - AC: mutating one cue in a fixture and saving leaves every other byte of the file identical; a mutation that would break segment coverage is refused with a structured error, never written; property test over random edit sequences never produces a document that fails the guard.
- [x] **M2.2 Undo/redo.** Single undo stack for every document mutation, with coalescing of consecutive typing into one entry.
  - AC: any sequence of edits can be undone back to the exact original bytes and redone forward to the exact edited bytes; undo depth is bounded and documented; typing a word is one undo step, not one per character.
- [x] **M2.3 Cue list UI with editing.** Virtualized cue list (index, start, end, text), inline text editing, keyboard navigation, dirty state, save/save-as.
  - AC: E2E: open the 2000-cue fixture, edit a cue's text, save, reopen, the edit is there and the rest is byte-identical; undo restores it; scrolling and typing show no visible lag (measured, budget CONTRIBUTING.md §7: open under 1 s).
  - **Status 2026-09-02:** the five tests carrying this milestone's own acceptance sentence lived behind the `real-media` cargo feature, and CI enabled it nowhere and generated the fixtures nowhere, so from W2 landing until today they had never run there. `cargo test` on this crate was green and was not saying what it looked like it was saying. They run on Linux on every push now, with a guard that fails the step if the named test is no longer in the suite, because a feature that stops being passed compiles the file away and leaves a green step behind.

- [x] **M2.4 Waveform.** Audio peaks extracted from the media (via the existing libmpv/ffmpeg path, off the main thread, cancellable) and rendered as a zoomable waveform with the playhead.
  - AC: peaks for a 60 s fixture appear within budget and match the audio (silence reads flat, the 440 Hz tone reads full); playhead tracks playback; zoom and scroll stay responsive; no main-thread blocking.
  - AC (decision 12, audio provider): reuse the ASR *pattern* — ffmpeg discovery, background execution, progress, cancellation — and not its code. Extraction runs at full quality behind a public API with a per-episode cache, and its lifetime is tied to the episode, not to a transcription run. The ASR extractor is private, produces mono 16 kHz because whisper wants that (`sublore-asr/src/sidecar.rs:285,302-304`), and writes into a scratch folder that deletes itself when the run ends (`scratch.rs:88-91`): fine for peaks, wrong for playing a selection, and it would vanish mid-session.
  - **Closed 2026-09-03, verified on Linux, ten tasks of ten (W1 to W10).** The panel draws peaks the audio crate produces off the main thread, the sash stores its height, zoom and follow keep the playhead on screen, the Audio menu lists the tracks and switching redraws, a media file with no audio says so instead of drawing nothing, and the peaks cache is wired to the thing that reads it. Both numbers CONTRIBUTING.md section 7 claims are measured by a check that can fail: first peaks under two seconds is guarded in CI on every push, and the 24-minute read is the owner's machine's (4436 ms cold, 512 ms warm). Windows compiles and has run none of it.
- [ ] **M2.5 Timing against the waveform.** Drag cue boundaries on the waveform, nudge with keyboard, snap to playhead; changes flow through the M2.1 mutation API and undo stack.
  - AC: E2E: drag a cue boundary, the model times change accordingly and save round-trips; nudge shortcuts move by the documented step; every timing change is undoable.
  - AC (filed 2026-08-29): the keyboard command set is what makes timing fast, and dragging alone does not cover it. Previous/next line, play selection, play line, play before/after/begin/end of selection, play to end, lead in, lead out, and commit each have a shortcut and each is exercised by a behavioural test.
  - AC (decision 4, one undo per operation): `sublore-edit` grows a composite history entry — a transaction of N child edits under a single label. Today an entry carries one `Splice` (`history.rs:37-39`), `Edit` names a single cue (`plan.rs:28-58`), and coalescing needs matching label and offset (`history.rs:192-195`), so edits on different cues never merge. Test: edit scattered rows, one undo returns the document byte-identical. Range variants were rejected because sparse selections, not contiguous ranges, are the real case.
  - **First slice done 2026-09-04, verified on Linux: dragging a boundary.** The cursor's cue puts two markers on the waveform and either can be grabbed, eight device pixels wide converted to milliseconds at the current zoom, taking the nearer of the two. What the document is told is the release and never the travel, through `subtitle_set_times`, so a whole drag is one undo step. A release that would put the end at or before the start is refused rather than clamped, because nothing below clamps it either. Six behavioural checks.
  - Note (decision 11): the product reasons in **milliseconds**, final. No frame, framerate or keyframe vocabulary, and no timing item carries a "pending a frame engine" reservation. If a frame seam is ever needed it lives in the player and nowhere else.
- [ ] **M2.6 Source/target side by side.** Two documents open at once (source and target), aligned by index, editing only the target.
  - AC: open two fixtures as source and target; rows align; editing the target never mutates the source file on disk; saving writes only the target.
  - AC (decision 13, how a translation is born): a **New translation from source** command creates a document inheriting the source's cues and timings with empty text; the source is read-only while translating; the first save asks for name and location with a sensible proposal (episode plus language). The source is never modified or overwritten. E2E: open only a source fixture, run the command, every cue and timing is carried over with empty text, translate two lines, save: a new file appears at the chosen path and the source fixture is byte-identical to before.
  - AC (decision 7, subtitles on video): the video shows the **translation**, with a toggle to show the source instead. The preview is fed from a shadow copy in the working folder; the user's file is **never** saved to produce a preview. E2E: with both documents open, pause on a time where a cue is active — the translation text is on the frame; toggle, and the source text is; neither file on disk changes, and no backup is created by toggling or typing.

**Status M2 part A, 2026-08-28:** M2.1-M2.3 on `main`. New crate `sublore-edit` (splice-based mutation planning, verification, undo/redo with explicit run boundaries, edit sessions). 289 Rust tests and 17 E2E checks green; the 2000-cue fixture opens in 68 ms against a 1000 ms budget with 26 rows in the DOM. Review caught and fixed three real defects: ASS cue deletion refused between blank runs, undo coalescing two deliberate edits into one step, and a global Ctrl+Z stealing native undo from text inputs. M2.4-M2.6 not started.

**Owner checklist M2:** open a real subtitle file with its video → edit some lines → adjust a cue against the waveform → undo a few times → save → reopen and confirm your edits are there and nothing else changed. Subtitle a 1-minute clip start to finish without another tool.

## M3 — Local transcription (tasks detailed 2026-08-28)

Goal: whisper.cpp sidecar producing editable, word-timestamped cues.

- [x] **M3.1 Sidecar process wrapper.** Run whisper.cpp as a child process (never in-process, never on the main thread): spawn, stream progress, cancel, reap. Build/vendor strategy for the binary decided and documented; CUDA never a hard dependency.
  - AC: a behavioral test runs a real transcription of a short audio fixture end to end and gets output; cancelling mid-run kills the child and leaves no orphan process (verified with a process check); the app stays responsive throughout; a missing or unrunnable binary produces a readable error, never a crash.
- [x] **M3.2 Model management.** Explicit, user-initiated model download with resume and integrity check; models live in the app data dir; nothing downloads on its own.
  - AC: no network request happens unless the user asks for a download (CONTRIBUTING.md §1); an interrupted download resumes instead of restarting; a corrupt or truncated file is detected by checksum and refused, never handed to whisper; models are never committed to the repo.
- [x] **M3.3 Word timestamps to cues.** Convert whisper output (word-level timestamps) into subtitle cues in the M1 document model, with a documented, deterministic segmentation rule.
  - AC: transcribing the fixture produces cues whose times are inside the media duration and strictly ordered; the same input always produces the same cues; the result is a valid document that passes the M1 coverage guard and can be saved as SRT and reopened byte-identically.
- [x] **M3.4 Transcription UI.** Choose model, start, see progress, cancel. Runs off the main thread with visible, cancellable progress (CONTRIBUTING.md §7).
  - AC: E2E: start a transcription on the fixture, progress advances, cancel stops it and the UI returns to a usable state with no orphan process; on completion the cues appear in the app; GPU/CPU selection is visible and CPU fallback works with no GPU present.

- [x] **M3.5 A finished transcription becomes the open document (filed 2026-09-01, owner ruling — wave 2).** `TranscribeBar` shows the cues a run produced and the run ends there: the result never becomes the document being edited, so the product's central flow, transcribe and then correct, is cut in half. The owner ruled the behaviour: the transcription becomes the open document. This is the most important of the three reachability holes.
  - AC: a transcription that finishes leaves its cues as the open document, editable in the grid, with the same undo the editor uses, and Save writes them.
  - AC: when a document with unsaved changes is already open, the user is asked the same question the close gate asks — save, discard, cancel — before the transcription replaces it, and cancel leaves both the old document and the transcription result intact (CONTRIBUTING.md §3, decision 9).
  - AC: nothing is written to disk by the transcription itself; the result exists in the session until the user saves it, and the media file is never touched (CONTRIBUTING.md §3.1).
  - AC: E2E drives it end to end: transcribe the fixture, edit a cue of the result, save, reopen, the edit is there.
  - Status 2026-09-01, verified-by-tests **on Linux**: the run keeps the SRT it already rendered, and `subtitle_adopt_transcription` parses it into a session with no path, which `History::unsaved()` makes dirty from birth. Nothing is written by the run; the E2E asserts the save directory is empty and the media untouched. Replacing unsaved work asks the close gate's own three-answer dialog, parameterised rather than copied, with save and replacement under one session lock so a failed save replaces nothing. Four E2E tests added, `EXPECTED_TESTS` 39 to 43; the CPU test gained the dialog answer its new route needs and kept every assertion it had.
  - Owed, and now closed (2026-09-01, decision 24 B2, verified-by-tests **on Linux**): the close gate's Save could not write a document that has no file. It asks for one now, through the save chooser, and writes there; cancelling that chooser still writes nothing and holds the window open with the work in it, which is decision 17's behaviour and the safe direction. The same first save is what the toolbar's Save and Ctrl+S do — the document adopts the path it is given and every save after it writes there, in place and with no chooser. Four E2E tests added, `EXPECTED_TESTS` 43 to 47, including the close gate's chooser driven through the window; the gate's *successful* first save is proved by the command bodies, not by the app, because writing then closing ends the session the suite drives. Two assertions in the M3.5 E2E that pinned the old refusal — Save disabled on a document with no file — now assert it is offered.

**Status 2026-08-28:** M3.1-M3.4 on `main` (developed in a parallel worktree, merged onto the editor and projects). New crate `sublore-asr`: whisper.cpp sidecar built by `scripts/build-whisper.sh` from the commit pinned in `whisper.pin` (never committed), model store with explicit resumable downloads, deterministic word-to-cue segmentation. 489 Rust tests, 27 E2E checks and the real-whisper suite (7 tests against the actual binary and model, transcription, cancellation, progress, determinism) all green. Review caught a model integrity hole: a corrupted model passed the length-only check and made whisper emit silently wrong text with exit 0; `resolve()` now verifies the sha256 and the UI offers a re-download. The pre-commit hook now blocks binaries, models and generated audio from ever entering the repo. Windows and CI unverified.

**Owner checklist M3:** download a model from inside the app → transcribe a short clip → watch progress → cancel one run and confirm nothing is left running → let one finish and see the cues appear, editable.

## M4 — Projects (tasks detailed 2026-08-28)

Goal: SQLite project (series → episodes → files) so memory has somewhere to live.

- [x] **M4.1 Schema and migrations.** One SQLite database file per project; schema for series, episodes, and the files attached to each episode (media path, subtitle paths, role); a versioned migration runner.
  - AC: creating a project produces a database at the chosen path with the current schema version; an automated test takes a database written at version N, migrates it, and verifies both the schema and every row survives (old db → migrate → verify, CONTRIBUTING.md §2); a database from a newer version than the app is refused with a readable error, never silently altered.
- [x] **M4.2 Project lifecycle.** Create, open, close a project; add episodes; attach existing media and subtitle files to an episode by path.
  - AC: create a project, add two episodes with files, close and reopen: everything is still there with the same paths and order; attaching a file records only its path and metadata, never copies or moves the user's file; opening a database that is corrupt or not a Sublore project fails with a readable error and leaves it untouched.
- [x] **M4.3 Deletion safety.** Deleting a project or an episode removes only Sublore's own records and its own project folder contents.
  - AC: a behavioral test with real files on disk deletes a project whose episodes reference media and subtitles outside the project folder, then asserts every one of those user files still exists byte-identical (CONTRIBUTING.md §3); no code path deletes outside the project folder.
- [x] **M4.4 Project UI, minimal.** Create/open a project, see its episodes and their attached files, add an episode, attach a file. No editing beyond that.
  - AC: E2E: create a project in a temp location, add an episode, attach a subtitle fixture, restart the app, reopen the project, the episode and its file are listed; errors surface as readable messages.

- [ ] **M4.5 An attached file opens (filed 2026-09-01, owner ruling).** `ProjectPanel` lists an episode's attached files as text. Nothing happens when one is clicked, so a project is a record of paths rather than a way into the work, and the user still opens every file through the chooser as if the project did not exist.
  - AC: activating an attached file opens it — a subtitle as the document, a video in the player — through the same commands the chooser route uses.
  - AC: a file that has moved or been deleted since it was attached says so in a readable message and changes nothing; the episode keeps its record and the app stays usable.
  - AC: opening a file this way honours the unsaved-changes question exactly as the chooser route does.
  - AC: E2E drives it: attach a subtitle to an episode, activate it, the cues are on screen; delete the file, activate it again, the message appears and the app is still usable.

**Status 2026-08-28:** M4.1-M4.4 on `main` (developed in a parallel worktree, merged onto M2 part A). New crate `sublore-project` (SQLite store, versioned migrations, project lifecycle, deletion safety). 359 Rust tests and 22 E2E checks green after integration. Review caught two critical races in `Database::create`, both reproduced before the fix: a symlink could redirect the project database outside the folder the user chose, and two concurrent creates could delete the winner's finished project; both closed with an atomic claim plus `SQLITE_OPEN_NOFOLLOW`. Merge integration moved the project panel into a left sidebar so the cue list keeps its height. Windows and CI unverified.

**Owner checklist M4:** create a project → add an episode → attach a subtitle file and a video → close the app → reopen the project and find everything where you left it → delete the project and confirm your own video and subtitle files are still on disk.

## M5 — Termbase + QA (pro, closed module)

Goal: the product. Per-project glossary with approved renderings; QA pass flags every target line where a source term appears without its approved rendering.
Draft criteria: on the standard fixture episode, QA flags exactly the planted violations and nothing else; term added mid-series retro-flags earlier episodes on demand; module absent → core runs untouched, module present + licensed → features appear.

**Constraint (decision 6, 2026-08-29):** the text matcher and the ASS override-tag scanner live in an **open-core crate**, and M5 consumes it. The closed module keeps only persistence (TM and termbase storage) and QA policy. Search and QA share fixtures. The comparison both need is identical — find a source term in a line while ignoring override tags — and CONTRIBUTING.md §4 requires the open core to be fully useful alone. Two engines would leave the free product unable to search, and the two would disagree about what counts as a match.

**Prerequisite:** N3 must be answered before this milestone starts.

## MW — Windows activation (mandatory before any sale or public release)

Goal: make Windows a verified platform instead of a compiled one. Filed 2026-08-29 with the platform policy change in CONTRIBUTING.md; decision 3 puts the scattered Windows work here rather than inside feature milestones, where half-finished platform work would spread across all of them.

Today the `check` job builds on ubuntu and windows (`.github/workflows/ci.yml:18`), but the behavioural suite runs on ubuntu only (`:125-126`) and drives the app with `xdotool` over XTEST while inspecting windows with `xwininfo` (`e2e/lib/input.js:7-10`) — neither exists on Windows. So every behavioural verdict in this file covers Linux and nothing else.

**What decision 24 G1 means for CI, and it is not free.** There is no separate Windows box and there will not be one: Windows 11 x64 is a dual boot on the owner's own machine, the self-hosted runner is powered up on demand, and MW.4's no-GPU case runs in a KVM virtual machine on Fedora. Hosted `windows-latest` stays a compile-only check on every push. **The Windows behavioural battery therefore runs at gates and tags, not on every push**, so a Windows regression can be up to a milestone old by the time anything catches it. Two consequences to plan around rather than discover: the commit that broke Windows has to be found by bisecting a milestone's worth of history, and Linux stays the only platform where a red build points at the change that made it red.

- [x] **MW.1a Say which part of the harness is Linux, and give the rest a seam (done 2026-09-02, verified on Linux).** Every file under `e2e/lib` and `e2e/tools` read and sorted into portable, Linux by nature, or Linux by accident. The Linux-by-nature ones now refuse by name on any other platform through `e2e/lib/platform.js`'s `requireLinuxBackend(seam, owes)`, each naming what a Windows counterpart has to do, so MW.1b fills a seam in rather than restructuring around one. No Windows code was written: none of it could have been run, and an unrun implementation reported as done is what the platform policy forbids.
  - **Portable already:** `e2e/lib/applog.js` (the data home arrives as an argument), `e2e/lib/tally.js`, `e2e/lib/env.js` (portable mechanism, and all three of its values are inert on Windows: nothing there reads `GDK_BACKEND` or `WAYLAND_DISPLAY`, and `main.rs` gates the webkit hatch behind `cfg(target_os = "linux")`), `e2e/tools/whisper-stub.mjs` (plain Node; only the shebang that runs it is Linux), and the new `e2e/lib/platform.js`. `waitFor` in `e2e/lib/proc.js` belongs here too.
  - **Linux by nature, seamed for MW.1b:** `e2e/lib/x11.js` (all of it, at the `xwininfo` chokepoint), `e2e/lib/input.js` (all of it, at the `xdotool` chokepoint), `e2e/lib/proc.js` `processGroupMembers` and `killGroup` (POSIX process groups), `e2e/lib/driver.js` `startDriver` (WebKitWebDriver, plus the process group `stopDriver` kills through), `e2e/lib/pixels.js` `screenGrabArgs` (only the `x11grab` input; the saturation measure above it is the same anywhere), `e2e/lib/asr.js` `appDataDir`, `installStubSidecar` and `processLine`, and `e2e/tools/close-window.py` (an ICCCM `WM_DELETE_WINDOW` ClientMessage).
  - **Linux by nature, no seam of their own on purpose:** `e2e/lib/gtk-dialog.js` and `e2e/lib/chooser.js`. Their window-finding half is generic and their keystrokes are GTK's (Alt mnemonics, Alt+Home, Ctrl+L, Alt+O); every path through them reaches `x11.js` or `input.js` first, so a second guard would be noise rather than safety. Both headers now say what MW.1b owes them.
  - **Linux by accident, fixed here:** one, in `e2e/lib/asr.js`. The model lookup falls back to `$HOME/.cache` and its failure message named only `XDG_CACHE_HOME` and `HOME`, neither of which Windows sets; it now names `SUBLORE_TEST_MODEL_DIR`, which is the platform-neutral way in and already existed.
  - AC, met: `pnpm lint` and `pnpm format:check` exit 0; the full wdio suite (9 spec files, 53 tests) and every script check that imports a touched library are green on Linux; `pixels.js`'s `saturation()` returns the same number before and after the extraction against the same painted display (118 on red, 0 on black); and all nine seams were made to fire, each with its own message. Verified on Linux, on Fedora with Xvfb and on the owner's Wayland session. No Windows result is claimed.
- [ ] **MW.1b The Windows implementations behind those seams.** Needs the dual boot of decision 24 G1 to exist, because every item is written by running it. Window inspection to replace `xwininfo` (toplevels by name and geometry, map state, children, and the app's own window picked out of them); synthetic input to replace `xdotool` over XTEST (real key, button and pointer-motion events, and moving the input focus); a process-group equivalent for `processGroupMembers` and `killGroup` (a job object is the unit); `startDriver` against the WebView2 driver rather than WebKitWebDriver, stopping the whole tree; `gdigrab` behind `pixels.js`'s `screenGrabArgs`; a shim the app can spawn as `SUBLORE_WHISPER_BIN`, since a `.mjs` has no shebang there; `processLine` without `ps`, still able to tell a reaped child from an unreaped one; a `WM_CLOSE` sibling for `close-window.py`; and Windows key tables for `gtk-dialog.js` and `chooser.js`, which face `dialog.rs`'s non-Linux arm and the common item dialog rather than GTK.
  - Also owed, in files MW.1a did not own: `e2e/lib/paths.js` needs `appBinary` to carry `.exe`, `requireTool` to stop shelling out to `sh -c command -v`, `requireDisplay` to mean something other than `DISPLAY`, and `closeWindowTool` to become the place that picks between the two close tools. `firstCueText` is a coordinate measured on one Linux window and will need its own Windows measurement.
  - AC: the full behavioural suite runs on Windows and is green, with the same spec files and the same assertions as on Linux. A failure on either platform turns the gate red.
- [ ] **MW.1c Redirect the app's data home on Windows.** The harness points a run at a throwaway data home by setting `XDG_DATA_HOME`, which is how `wdio.conf.js`, the close-gate checks, `applog.js` and `asr.js` all find what the app wrote. Windows resolves `app_data_dir()` from a known folder id and no environment variable moves it, so on Windows those checks would read the real profile or nothing at all. Whatever replaces it touches `src-tauri` and `e2e/wdio.conf.js`, so it is its own change and not a harness edit.
  - AC: a Windows run writes its logs, backups and scratch under a directory the run created and deletes, and a check can read them from there. Running the suite twice leaves nothing behind in the user's profile.
- [ ] **MW.2 Platform hardening.** Work through what only Windows can show: the native video surface z-order (`video/surface/windows.rs` reasserts `HWND_TOP`), path and encoding handling, the crash dialog, the installer.
  - AC: the M0.2, M0.4, N1, N2 and M2.0 criteria are re-run on Windows and pass there, including the occlusion behaviour of decision 1.
- [ ] **MW.4 The dependency graph on a machine without a GPU driver.** `libmpv-2.dll` imports `vulkan-1.dll` statically, not through `GetProcAddress`, so the loader has to resolve it before `main` runs even though Sublore never asks mpv for Vulkan. A Windows machine whose only display adapter is the Microsoft Basic one may have no Vulkan loader at all, and the app would die at startup with no message. Filed 2026-08-31 while diagnosing the `STATUS_ENTRYPOINT_NOT_FOUND` exits of the three Windows CI test targets that link mpv; the runner turned out to have a complete loader, so this is untested rather than known-broken.
  - AC: on a Windows install with no GPU vendor driver, Sublore starts, opens a subtitle and plays a video, or says what is missing in a sentence the user can act on. A silent death at load is a failure.

- [ ] **MW.3 Owner checklist on Windows.** Every owner checklist in this file, run by the owner on a Windows machine from an installed build.
  - AC: the owner signs off. Until then no build ships to anyone.

**This milestone gated release until 2026-09-04, when the owner ruled that v1.0 ships on Linux alone.** It still gates a Windows build: nothing is sold or handed to anyone as a Windows product while it is open. The Linux tag no longer waits for it. See the platform policy in CONTRIBUTING.md.

## M6 — Translation memory + licensing + release

Goal: exact and fuzzy TM across the whole project; offline license check; pay-once purchase flow via merchant of record; v1.0 tag.
Draft criteria: repeated lines in later episodes surface their earlier translation; fuzzy matches ranked and insertable; license file validates offline, invalid license degrades gently to free core; both installers pass the full owner checklist end to end.

**Prerequisite:** none on MW since the 2026-09-04 ruling. The v1.0 Linux tag no longer waits for Windows activation; a Windows build still does.

---

## Parking lot (explicitly not v1 — do not pull forward)

- **Should the CI runner have a minimal window manager? (parked by owner ruling 2026-08-31.)** The first real CI run failed `close-gate-check.js` on `XSetInputFocus` answering `BadMatch`, which is what a bare X server with nothing to hold focus looks like. **For:** users have a window manager, so a runner without one tests an environment nobody has, and focus, stacking and close buttons all behave differently there. **Against:** several checks were built against the WM-less runner and read its behaviour as the baseline — the close gate drives `WM_DELETE_WINDOW` directly, the surface checks read raw stacking — so adding one reopens every one of them at once. Not decided inside gate 2 for that reason. Whoever picks it up starts by listing which checks change meaning under a WM, because that list is the cost.

- **"Still saving" indicator after a threshold (decision 17, 2026-08-31).** The close gate waits on the session lock without a timeout, by ruling, because every automatic release costs the user's work. What is owed is not a bound but a voice: past some threshold the window says it is still saving, so an unbounded wait stops being a silent one.

- **External VAD with timestamp remapping (decision 16, 2026-08-30).** Cue-boundary tuning, done outside whisper.cpp and mapped back onto the transcript's timings. The measured lever is segmentation, not the model and not source separation (`asr-anime.md`), but the built-in Silero is ruled out for this domain, so the instrument is external. Post-v1 pipeline task. **No proof of concept before the parking lot promotes it.**
- **Chinese punctuation as a post-ASR pass (decision 15, 2026-08-30).** Beside re-punctuation. The one shippable in-architecture win the sweep found was a punctuating Mandarin fine-tune, and decision 15 declines it: a per-language model is a UI, a download, an in-app licence and a verification matrix.

Karaoke/typesetting · diarization · built-in LLM translation beyond BYOK hook · macOS activation · auto-updater · batch CLI · Rust-side localized strings for the native crash dialog (v1 ships it English-only, owner decision 2026-08-23). Anything anyone wants to add lands here first and waits for the owner.
